---
phase: 12-schedule-based-grouping
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - Sources/ActiveJobsCoreSelfTest/main.swift
autonomous: true
requirements: [GROUP-02, GROUP-03, GROUP-04]

must_haves:
  truths:
    - "Scheduled jobs with nextRun at different hours are classified into correct time-of-day buckets"
    - "Scheduled jobs with frequency keywords (but no nextRun) are classified into correct frequency buckets"
    - "Registered, Candidate, and Manual jobs always land in 'No schedule evidence' regardless of schedule text"
    - "Clock-time fallback parsing works correctly for schedule strings like '01:00, 04:00'"
    - "testGroupingModeShortcutKeys updated to expect 6 cases with .schedule mapped to Cmd+6"
    - "All existing self-tests continue to pass — no regressions"
  artifacts:
    - path: "Sources/ActiveJobsCoreSelfTest/main.swift"
      provides: "Comprehensive test coverage for schedule classifier and grouping"
      contains: "testScheduleClassifierTimeOfDay"
      contains: "testScheduleClassifierFrequency"
      contains: "testScheduleClassifierEvidenceBoundaries"
      contains: "testScheduleGroupingIntegration"
  key_links:
    - from: "testScheduleClassifierTimeOfDay"
      to: "SidebarScheduleClassifier.kind(for:)"
      via: "JobPresentation with nextRun fixtures"
    - from: "testScheduleClassifierEvidenceBoundaries"
      to: "JobConfidence.candidate / .registered / .manual"
      via: "confidence != .scheduled → .noScheduleEvidence"
---

<objective>
Add comprehensive self-test coverage for the schedule-based grouping
classifier: time-of-day bucket assignment, frequency keyword detection,
evidence boundary preservation (non-scheduled → "No schedule evidence"),
and end-to-end grouping integration. Also updates the existing
testGroupingModeShortcutKeys to account for the 6th grouping mode.

Purpose: Verify that the classification heuristics from Plan 01 produce
correct results and that the evidence boundary (Pitfall 3) is airtight.

Output: Four new self-test functions plus one updated existing test,
all passing in `swift run ActiveJobsCoreSelfTest`.
</objective>

<execution_context>
@/Users/niko/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/niko/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-schedule-based-grouping/12-CONTEXT.md
@.planning/research/PITFALLS.md  (Pitfall 3: Evidence Boundary Violation)
@.planning/phases/12-schedule-based-grouping/12-01-PLAN.md

<interfaces>
<!-- Types created by Plan 01 that tests exercise -->

From Sources/AutomationHealthCore/JobPresentation.swift — new types:
```swift
public enum SidebarScheduleKind: String, CaseIterable, Identifiable, Sendable {
    case morning, afternoon, evening, night
    case hourly, daily, weekly, monthly
    case noScheduleEvidence
    public var title: String { ... }
}

// Private but testable through public API:
// SidebarScheduleClassifier.kind(for: JobPresentation) -> SidebarScheduleKind
// groupDefinitions(for: .schedule) -> [SidebarGroupDefinition]
// SidebarGroupingMode.schedule — new 6th case
```

Test patterns from existing self-tests:
- `ScheduledJob.fixture(...)` — creates test job records
- `JobPresentation(job:)` — wraps ScheduledJob for classification
- `SidebarJobSection.sections(for:groupingMode:collapseState:hasSearchQuery:)` — grouping integration
- `section(_:titled:)` — extracts a specific section from section array
- `expect(_:_:)` — boolean assertion
- Test function format: top-level `try testXxx()` invocation at top of file, function definition follows
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update testGroupingModeShortcutKeys for 6 grouping modes</name>
  <files>Sources/ActiveJobsCoreSelfTest/main.swift</files>

  <read_first>
Read Sources/ActiveJobsCoreSelfTest/main.swift lines 1082-1104 (testGroupingModeShortcutKeys).
Focus on the assertion structure: allCases count, index access, label checks.
Read also lines 1-42 (top-level try invocations) to verify test ordering.
</read_first>

  <behavior>
    The existing test expects exactly 5 cases. After Plan 01 adds .schedule as the 6th case,
    the test must be updated to expect 6 cases with Cmd+6 → schedule.
  </behavior>

  <action>
Edit `testGroupingModeShortcutKeys()` (starting at line 1082):

1. Change the count assertion from 5 to 6:
   ```swift
   // Change: expect(allModes.count == 5, ...)
   // To:
   expect(allModes.count == 6, "Exactly 6 grouping modes for Cmd+1..6 shortcuts")
   ```

2. Add a new assertion after the Cmd+5 check for `.schedule` (line 1093):
   ```swift
   expect(allModes[5] == .schedule, "Cmd+6 maps to Schedule grouping")
   ```

3. Add a label check for the `.schedule` mode (after line 1100):
   ```swift
   expect(allModes[5].label == "Schedule", "Schedule mode label is 'Schedule'")
   ```

4. Update the comment on the count assertion line from "Cmd+1..5" to "Cmd+1..6":
   ```swift
   expect(allModes.count == 6, "Exactly 6 grouping modes for Cmd+1..6 shortcuts")
   ```

No other changes. The existing index-based assertions for modes 0-4 and labels
0-4 are unaffected since `.schedule` is appended at index 5.
  </action>

  <verify>
    <automated>swift run ActiveJobsCoreSelfTest</automated>
  </verify>

  <acceptance_criteria>
- `grep "Exactly 6 grouping modes" Sources/ActiveJobsCoreSelfTest/main.swift` returns a match
- `grep "Cmd+6 maps to Schedule" Sources/ActiveJobsCoreSelfTest/main.swift` returns a match
- `grep "Schedule mode label is 'Schedule'" Sources/ActiveJobsCoreSelfTest/main.swift` returns a match
- `swift run ActiveJobsCoreSelfTest` — testGroupingModeShortcutKeys passes (no fatalError)
  </acceptance_criteria>

  <done>
testGroupingModeShortcutKeys now expects 6 grouping modes with .schedule at index 5
mapped to Cmd+6. All 6 grouping mode labels are validated.
</done>
</task>

<task type="auto">
  <name>Task 2: Add schedule classifier tests and grouping integration tests</name>
  <files>Sources/ActiveJobsCoreSelfTest/main.swift</files>

  <read_first>
Read Sources/ActiveJobsCoreSelfTest/main.swift:
- Lines 1-42 (top-level try invocations) — for adding new test calls
- Lines 538-561 (testSidebarTriggerClassificationPreservesEvidenceBoundaries) — pattern reference for evidence boundary testing
- Lines 517-537 (testSidebarOriginGroupingSplitsLaunchdRows) — pattern reference for grouping integration
- Lines 1198-1236 (ScheduledJob.fixture extension) — fixture builder API
- Lines 1106-1140 (expect, section, isoDate helpers) — assertion helpers

Study the fixture builder signature:
```swift
ScheduledJob.fixture(
    id: String,
    name: String,
    source: JobSource = .hermesCron,
    confidence: JobConfidence = .scheduled,
    schedule: String = "0 10 * * *",
    nextRun: Date? = nil,
    ...
)
```

Key: `nextRun` is a Date? — set it for time-of-day tests; leave nil for frequency/fallback tests.
`schedule` is the raw string — set for clock-time fallback (D-06) and frequency keyword tests.
`confidence` gates classification — use .candidate/.registered/.manual for boundary tests.
</read_first>

  <behavior>
    - Test A (timeOfDay): nextRun with hour=8 → .morning; hour=14 → .afternoon; hour=20 → .evening; hour=0 → .night
    - Test B (clockTime fallback): no nextRun, schedule="01:00, 04:00" → earliest hour=1 → .night; schedule="12:00" → .afternoon
    - Test C (frequency): no nextRun, schedule="0 * * * *" (every hour cron) → scheduleDescription="Every hour" → .hourly; schedule="0 10 * * *" → scheduleDescription="Daily at 10:00" → .daily
    - Test D (evidence boundaries): confidence=.registered with schedule "Daily at noon" → .noScheduleEvidence; .candidate → .noScheduleEvidence; .manual → .noScheduleEvidence; .scheduled with empty schedule → .noScheduleEvidence
    - Test E (grouping integration): sections(for:groupingMode:.schedule) produces correct sections with correct job IDs
    - Test F (empty section handling): no .scheduled jobs → only "No schedule evidence" section visible
  </behavior>

  <action>
Add four new test functions to `Sources/ActiveJobsCoreSelfTest/main.swift`,
plus their top-level `try` invocations.

### Step 1: Add top-level test invocations

After `try testGroupingModeShortcutKeys()` (line 40), add:
```swift
try testScheduleClassifierTimeOfDay()
try testScheduleClassifierFrequency()
try testScheduleClassifierEvidenceBoundaries()
try testScheduleGroupingIntegration()
```

### Step 2: Add test function implementations

Insert all four functions before the helper section (before `expect()` at line 1106).

#### testScheduleClassifierTimeOfDay — Time-of-day bucket assignment (D-05, D-06)

Test both paths:
- **Primary (D-05):** Create 5 `JobPresentation` fixtures with `.scheduled` confidence and
  `nextRun` set to known hours. Use `Calendar.current.date(from: DateComponents(...))`.
  Hours 8, 14, 20, 22, 2 should classify as Morning, Afternoon, Evening, Night, Night.
  Build `SidebarJobSection.sections(for:groupingMode:.schedule...)` and verify each
  section using `section(sections, titled:)`.

- **Fallback (D-06):** Create a job with `nextRun: nil` and `schedule: "12:00"`.
  Verify it classifies as Afternoon. Create a job with `schedule: "01:00, 04:00"`
  (multi-clock) and verify the earliest hour (1) classifies as Night.

Section title constants to use: `"Morning (4 AM – 12 PM)"`, `"Afternoon (12 PM – 6 PM)"`,
`"Evening (6 PM – 10 PM)"`, `"Night (10 PM – 4 AM)"`.

#### testScheduleClassifierFrequency — Frequency keyword detection (D-07)

Create 4 `JobPresentation` fixtures with `.scheduled` confidence, `nextRun: nil`, and
these schedule strings:

| schedule string | humanized result | classifier keyword match | expected bucket |
|----------------|-------------------|--------------------------|-----------------|
| `"Every hour"` | `"Every hour"` | contains "every hour" | `Hourly` |
| `"0 10 * * *"` | `"Daily at 10:00"` | contains "daily" | `Daily` |
| `"Every week"` | `"Every week"` | contains "every week" | `Weekly` |
| `"0 10 15 * *"` | `"Monthly on day 15 at 10:00"` | contains "monthly" | `Monthly` |

Build sections and verify each job lands in the expected section by title.
Note: Section titles are just `"Hourly"`, `"Daily"`, `"Weekly"`, `"Monthly"`
(no time ranges in frequency section titles).

#### testScheduleClassifierEvidenceBoundaries — Confidence gate (D-08, Pitfall 3)

Verify the critical evidence boundary: non-`.scheduled` jobs NEVER appear in
time-of-day or frequency sections.

Create fixtures:
1. `confidence: .registered`, `schedule: "Daily at noon"` → `"No schedule evidence"`
2. `confidence: .candidate`, `schedule: "0 10 * * *"` → `"No schedule evidence"`
3. `confidence: .manual`, `schedule: "Every Friday"` → `"No schedule evidence"`
4. `confidence: .scheduled`, `schedule: ""` (empty schedule, no keywords, no nextRun) → `"No schedule evidence"`

Verify all four land in `"No schedule evidence"` section.
Also verify: no other sections exist (only "No schedule evidence" is returned).

This directly proves Pitfall 3 is solved — a Candidate with "Daily at noon"
goes to "No schedule evidence", NOT "Daily".

#### testScheduleGroupingIntegration — End-to-end grouping + section ordering

Create a mixed bag of jobs across confidence levels:
- 1 `.scheduled` with `nextRun` at hour 8 (Morning)
- 1 `.scheduled` with `schedule: "0 10 * * *"` (Daily, no nextRun)
- 1 `.registered` (→ No schedule evidence)
- 1 `.candidate` (→ No schedule evidence)

Build `SidebarJobSection.sections(for:groupingMode:.schedule...)` and verify:
1. Section titles in order: `"Morning (4 AM – 12 PM)"`, `"Daily"`, `"No schedule evidence"`
   (time-of-day before frequency before no-evidence; D-04: no-evidence last)
2. Each section contains exactly the expected job IDs
3. No other sections present (verify section count == 3)
4. Empty sections are excluded — no `"Afternoon"`, `"Evening"`, `"Night"`, `"Hourly"`,
   `"Weekly"`, `"Monthly"` sections appear since no jobs classify into them

This confirms the full pipeline: classifier → group definitions → sections →
empty filtering → section ordering → collapse state integration.
  </action>

  <verify>
    <automated>swift run ActiveJobsCoreSelfTest</automated>
  </verify>

  <acceptance_criteria>
- `grep -c "testScheduleClassifierTimeOfDay" Sources/ActiveJobsCoreSelfTest/main.swift` returns 2 (invocation + definition)
- `grep -c "testScheduleClassifierFrequency" Sources/ActiveJobsCoreSelfTest/main.swift` returns 2
- `grep -c "testScheduleClassifierEvidenceBoundaries" Sources/ActiveJobsCoreSelfTest/main.swift` returns 2
- `grep -c "testScheduleGroupingIntegration" Sources/ActiveJobsCoreSelfTest/main.swift` returns 2
- `swift run ActiveJobsCoreSelfTest` outputs "ActiveJobsCoreSelfTest passed" with exit code 0
- No `fatalError` from any test (all expectations pass)
  </acceptance_criteria>

  <done>
Four new self-test functions:
1. testScheduleClassifierTimeOfDay: verifies nextRun hour bucket assignment (Morning/Afternoon/Evening/Night) + clock-time regex fallback + multi-clock earliest match
2. testScheduleClassifierFrequency: verifies scheduleDescription keyword detection (hourly/daily/weekly/monthly)
3. testScheduleClassifierEvidenceBoundaries: verifies .registered/.candidate/.manual always → noScheduleEvidence; .scheduled with no classifiable text → noScheduleEvidence
4. testScheduleGroupingIntegration: verifies sections(for:groupingMode:.schedule) produces correct sections, correct job-to-section mapping, "No schedule evidence" is last section, empty sections are excluded
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. Tests exercise pure in-memory classification logic.

| Boundary | Description |
|----------|-------------|
| N/A | Self-tests run in-process with no external dependencies |
</threat_model>

<verification>
1. `swift build` — all test code compiles
2. `swift run ActiveJobsCoreSelfTest` — all 37 tests pass (33 existing + 4 new + updated shortcut test = 38 total; verify output line count)
3. `grep "ActiveJobsCoreSelfTest passed"` — final output line confirms success
4. Verify no regressions: existing tests for trigger classification, grouping modes, collapse state, type-to-select all still pass
</verification>

<success_criteria>
- [ ] testGroupingModeShortcutKeys updated to expect 6 cases
- [ ] testScheduleClassifierTimeOfDay: verifies Morning (04-11), Afternoon (12-17), Evening (18-21), Night (22-03)
- [ ] testScheduleClassifierTimeOfDay: verifies clock-time fallback from schedule string
- [ ] testScheduleClassifierFrequency: verifies hourly, daily, weekly, monthly keyword detection
- [ ] testScheduleClassifierEvidenceBoundaries: verifies .registered/.candidate/.manual → .noScheduleEvidence
- [ ] testScheduleClassifierEvidenceBoundaries: verifies Pitfall 3 is solved — Candidate with "Daily at noon" schedule goes to "No schedule evidence", not "Daily"
- [ ] testScheduleGroupingIntegration: verifies sections(for: .schedule) produces expected section titles and job counts
- [ ] testScheduleGroupingIntegration: verifies "No schedule evidence" is always the last rendered section
- [ ] `swift run ActiveJobsCoreSelfTest` exits 0 with "ActiveJobsCoreSelfTest passed"
</success_criteria>

<output>
After completion, create `.planning/phases/12-schedule-based-grouping/12-02-SUMMARY.md`
</output>
