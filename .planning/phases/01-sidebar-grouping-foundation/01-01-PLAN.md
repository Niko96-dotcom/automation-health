---
phase: 01-sidebar-grouping-foundation
plan: 01-01
type: execute
wave: 1
depends_on: []
files_modified: [Sources/AutomationHealth/Models/JobPresentation.swift]
autonomous: true
requirements: [ORG-01, ORG-02, ORG-03, ORG-05, SRCH-01, QUAL-03]
user_setup: []
must_haves:
  truths:
    - "D-01: Sidebar source sections are derived by iterating JobSource.allCases, preserving launchd before Hermes cron."
    - "D-02: Jobs inside each source section preserve the incoming visible JobPresentation order."
    - "D-03: SidebarJobSection.sections(for:) returns no section for a source with zero visible jobs."
    - "D-04: Each SidebarJobSection exposes a human-readable source title and visibleCount."
    - "D-05: Grouped SidebarJobSummary subtitles omit the job source name and use next-run text or schedule text."
    - "D-06: The section builder accepts already-visible filtered jobs, so section counts reflect only the visible input."
    - "D-07: The same visible section data can be summed for the top sidebar summary count."
  artifacts:
    - path: "Sources/AutomationHealth/Models/JobPresentation.swift"
      provides: "Grouped sidebar presentation model and source-free grouped row summaries"
      contains: "struct SidebarJobSection"
  key_links:
    - from: "Sources/AutomationHealth/Models/JobPresentation.swift"
      to: "Sources/ActiveJobsCore/Models/ScheduledJob.swift"
      via: "JobSource.allCases drives section ordering"
      pattern: "JobSource\\.allCases"
---

<objective>
Create grouped sidebar presentation data from visible `JobPresentation` values.

Purpose: Give the sidebar a render-ready source-section model while keeping scanner IO out of views and preserving existing inventory order.
Output: `SidebarJobSection` plus grouped row-summary subtitle behavior in `Sources/AutomationHealth/Models/JobPresentation.swift`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-sidebar-grouping-foundation/01-CONTEXT.md
@.planning/phases/01-sidebar-grouping-foundation/01-RESEARCH.md
@.planning/phases/01-sidebar-grouping-foundation/01-UI-SPEC.md
@.planning/phases/01-sidebar-grouping-foundation/01-PATTERNS.md
@Sources/AutomationHealth/Models/JobPresentation.swift
@Sources/ActiveJobsCore/Models/ScheduledJob.swift
</context>

<threat_model>
Trust boundaries: This plan stays inside app-target presentation data and consumes already-scanned `ScheduledJob` values through `JobPresentation`.

STRIDE register:
- threat_id: P1-01-T1
  category: Tampering
  component: `SidebarJobSection.sections(for:)`
  disposition: mitigated
  mitigation_plan: Do not add filesystem writes, scanner calls, job mutation APIs, shell commands, persistence, or network calls. The helper only transforms `[JobPresentation]` into `[SidebarJobSection]`.
- threat_id: P1-01-T2
  category: Information Disclosure
  component: grouped subtitles
  disposition: mitigated
  mitigation_plan: Reuse existing humanized timing and schedule strings; do not expose command text, definitions, or output snippets in sidebar rows.
- threat_id: P1-01-T3
  category: Denial of Service
  component: grouping helper
  disposition: accepted-low
  mitigation_plan: Use simple in-memory filtering over the already-visible job list. No unbounded IO or recursive traversal is introduced.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add sidebar section presentation data</name>
  <files>Sources/AutomationHealth/Models/JobPresentation.swift</files>
  <read_first>Sources/AutomationHealth/Models/JobPresentation.swift, Sources/ActiveJobsCore/Models/ScheduledJob.swift, .planning/phases/01-sidebar-grouping-foundation/01-CONTEXT.md, .planning/phases/01-sidebar-grouping-foundation/01-UI-SPEC.md</read_first>
  <action>Add `struct SidebarJobSection: Identifiable, Hashable, Sendable` in `JobPresentation.swift`. Give it these immutable properties: `let id: JobSource`, `let source: JobSource`, `let title: String`, `let visibleCount: Int`, and `let jobs: [SidebarJobSummary]`. Add a static factory `static func sections(for visibleJobs: [JobPresentation]) -> [SidebarJobSection]` that iterates `JobSource.allCases.compactMap`, filters the input with `visibleJobs.filter { $0.job.source == source }`, returns `nil` when the filtered array is empty, and otherwise returns a section with `id: source`, `source: source`, `title: source.displayName`, `visibleCount: sourceJobs.count`, and `jobs: sourceJobs.map { SidebarJobSummary(job: $0, includesSourceName: false) }`. Do not sort either sources or jobs; source order must come only from `JobSource.allCases`, and row order must be the input order.</action>
  <verify>swift build</verify>
  <acceptance_criteria>
    - `Sources/AutomationHealth/Models/JobPresentation.swift` contains `struct SidebarJobSection: Identifiable, Hashable, Sendable`.
    - `Sources/AutomationHealth/Models/JobPresentation.swift` contains `static func sections(for visibleJobs: [JobPresentation]) -> [SidebarJobSection]`.
    - `Sources/AutomationHealth/Models/JobPresentation.swift` contains `JobSource.allCases.compactMap`.
    - `Sources/AutomationHealth/Models/JobPresentation.swift` contains `visibleJobs.filter { $0.job.source == source }`.
    - `Sources/AutomationHealth/Models/JobPresentation.swift` contains `SidebarJobSummary(job: $0, includesSourceName: false)`.
  </acceptance_criteria>
  <done>`SidebarJobSection.sections(for:)` creates source-ordered, empty-section-free grouped data from an already-visible job array.</done>
</task>

<task type="auto">
  <name>Task 2: Support grouped row subtitles without source duplication</name>
  <files>Sources/AutomationHealth/Models/JobPresentation.swift</files>
  <read_first>Sources/AutomationHealth/Models/JobPresentation.swift, .planning/phases/01-sidebar-grouping-foundation/01-CONTEXT.md, .planning/phases/01-sidebar-grouping-foundation/01-UI-SPEC.md</read_first>
  <action>Change `SidebarJobSummary.init(job:)` to `init(job: JobPresentation, includesSourceName: Bool = true)`. Inside the initializer, keep assigning `id`, `displayName`, and `healthKind` exactly as today. Add `let detailText = job.job.nextRun != nil ? job.nextRunText : job.scheduleText`. Set `subtitle = includesSourceName ? "\(job.sourceName) • \(detailText)" : detailText`. This preserves the legacy source-prefixed subtitle for any ungrouped caller while allowing `SidebarJobSection.sections(for:)` to produce source-free grouped row subtitles. Do not move schedule or health logic into `SidebarView`.</action>
  <verify>swift build</verify>
  <acceptance_criteria>
    - `Sources/AutomationHealth/Models/JobPresentation.swift` contains `init(job: JobPresentation, includesSourceName: Bool = true)`.
    - `Sources/AutomationHealth/Models/JobPresentation.swift` contains `let detailText = job.job.nextRun != nil ? job.nextRunText : job.scheduleText`.
    - `Sources/AutomationHealth/Models/JobPresentation.swift` contains `subtitle = includesSourceName ?`.
    - `Sources/AutomationHealth/Models/JobPresentation.swift` still contains `healthKind = job.health.kind`.
  </acceptance_criteria>
  <done>Grouped row summaries can omit source names while retaining the existing next-run-or-schedule subtitle rule.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `swift build` exits 0.
- [ ] `Sources/AutomationHealth/Models/JobPresentation.swift` contains `struct SidebarJobSection`.
- [ ] `SidebarJobSection.sections(for:)` uses `JobSource.allCases` and does not contain `.sorted`.
- [ ] `SidebarJobSummary` supports `includesSourceName: Bool = true`.
</verification>

<success_criteria>
- Grouped sidebar data is created from visible `JobPresentation` values only.
- Empty source groups are omitted by the presentation helper.
- Source order follows `JobSource.allCases`.
- Job order inside each section is unchanged from the visible input array.
- Grouped row subtitles omit duplicated source names.
</success_criteria>

<output>
After completion, create `.planning/phases/01-sidebar-grouping-foundation/01-01-SUMMARY.md`.
</output>
