---
phase: 01-sidebar-grouping-foundation
plan: 01-03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified: [Sources/ActiveJobsCoreSelfTest/main.swift, Sources/AutomationHealth/Models/JobPresentation.swift, Sources/AutomationHealth/Views/ContentView.swift, Sources/AutomationHealth/Views/SidebarView.swift]
autonomous: true
requirements: [ORG-05, QUAL-03]
user_setup: []
must_haves:
  truths:
    - "D-01: A self-test asserts JobSource.allCases remains `[.launchd, .hermesCron]` for stable sidebar source ordering."
    - "D-02: The final build compiles the grouped section helper that preserves input order inside source groups."
    - "D-03: The final build compiles the grouped section helper that omits zero-job sources."
    - "D-04: The final build compiles source headers with title and visible count fields."
    - "D-05: The final build compiles grouped row summaries with source-free subtitles."
    - "D-06: The final build compiles filter-then-group wiring from ContentView."
    - "D-07: The final build compiles visible-count summary wiring from SidebarView sections."
  artifacts:
    - path: "Sources/ActiveJobsCoreSelfTest/main.swift"
      provides: "Source order regression check for sidebar grouping"
      contains: "testJobSourceOrderMatchesSidebarGroupingContract"
  key_links:
    - from: "Sources/ActiveJobsCoreSelfTest/main.swift"
      to: "Sources/ActiveJobsCore/Models/ScheduledJob.swift"
      via: "JobSource.allCases expectation"
      pattern: "JobSource\\.allCases"
---

<objective>
Add focused validation for grouping behavior and stable ordering.

Purpose: Protect the source-order contract through the existing self-test workflow and verify the app-target grouping code compiles cleanly under the local CI gate.
Output: A focused `JobSource` ordering self-test plus successful `swift build` and `./script/ci.sh` verification.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-sidebar-grouping-foundation/01-CONTEXT.md
@.planning/phases/01-sidebar-grouping-foundation/01-RESEARCH.md
@.planning/phases/01-sidebar-grouping-foundation/01-UI-SPEC.md
@.planning/phases/01-sidebar-grouping-foundation/01-PATTERNS.md
@.planning/phases/01-sidebar-grouping-foundation/01-01-SUMMARY.md
@.planning/phases/01-sidebar-grouping-foundation/01-02-SUMMARY.md
@Sources/ActiveJobsCoreSelfTest/main.swift
@Sources/ActiveJobsCore/Models/ScheduledJob.swift
@Sources/AutomationHealth/Models/JobPresentation.swift
@Sources/AutomationHealth/Views/ContentView.swift
@Sources/AutomationHealth/Views/SidebarView.swift
@script/ci.sh
@script/test.sh
</context>

<threat_model>
Trust boundaries: This plan changes only executable self-test coverage and runs local build/test commands. It does not add app runtime behavior.

STRIDE register:
- threat_id: P1-03-T1
  category: Tampering
  component: self-test source
  disposition: mitigated
  mitigation_plan: Add assertions only; do not write scheduler files outside existing temporary fixture helpers and do not modify live scheduler jobs.
- threat_id: P1-03-T2
  category: Denial of Service
  component: local CI gate
  disposition: accepted-low
  mitigation_plan: Use existing bounded `swift build`, `swift run ActiveJobsCoreSelfTest`, and `./script/ci.sh` commands. Do not add long-running watchers or background processes.
- threat_id: P1-03-T3
  category: Repudiation
  component: validation evidence
  disposition: mitigated
  mitigation_plan: Record exact commands and outcomes in the plan summary so failed validation cannot be mistaken for completed work.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add source order self-test</name>
  <files>Sources/ActiveJobsCoreSelfTest/main.swift</files>
  <read_first>Sources/ActiveJobsCoreSelfTest/main.swift, Sources/ActiveJobsCore/Models/ScheduledJob.swift, .planning/phases/01-sidebar-grouping-foundation/01-CONTEXT.md</read_first>
  <action>Add a top-level call `try testJobSourceOrderMatchesSidebarGroupingContract()` near the existing self-test calls. Add a function `func testJobSourceOrderMatchesSidebarGroupingContract() throws` that asserts `JobSource.allCases == [.launchd, .hermesCron]` and `JobSource.allCases.map(\\.displayName) == ["launchd", "Hermes cron"]` using the existing `expect` helper. Keep the test synchronous and throwing like the existing tests. Do not introduce XCTest, Swift Testing, a new test target, or app-target imports.</action>
  <verify>swift run ActiveJobsCoreSelfTest</verify>
  <acceptance_criteria>
    - `Sources/ActiveJobsCoreSelfTest/main.swift` contains `try testJobSourceOrderMatchesSidebarGroupingContract()`.
    - `Sources/ActiveJobsCoreSelfTest/main.swift` contains `func testJobSourceOrderMatchesSidebarGroupingContract() throws`.
    - `Sources/ActiveJobsCoreSelfTest/main.swift` contains `JobSource.allCases == [.launchd, .hermesCron]`.
    - `Sources/ActiveJobsCoreSelfTest/main.swift` contains `JobSource.allCases.map(\\.displayName) == ["launchd", "Hermes cron"]`.
  </acceptance_criteria>
  <done>The existing self-test workflow fails if the source order contract used by sidebar grouping changes accidentally.</done>
</task>

<task type="auto">
  <name>Task 2: Run app compile validation</name>
  <files>Sources/AutomationHealth/Models/JobPresentation.swift, Sources/AutomationHealth/Views/ContentView.swift, Sources/AutomationHealth/Views/SidebarView.swift</files>
  <read_first>Sources/AutomationHealth/Models/JobPresentation.swift, Sources/AutomationHealth/Views/ContentView.swift, Sources/AutomationHealth/Views/SidebarView.swift, Package.swift</read_first>
  <action>Run `swift build` from the repository root. If compilation fails because of the Phase 1 grouping changes, fix the compile errors in the Phase 1 files only: `JobPresentation.swift`, `ContentView.swift`, `SidebarView.swift`, or `ActiveJobsCoreSelfTest/main.swift`. Do not change scanner services, detail view behavior, scheduler IO, package dependencies, or unrelated dirty worktree files.</action>
  <verify>swift build</verify>
  <acceptance_criteria>
    - `swift build` exits 0 after the grouping changes.
    - `Package.swift` still has no package dependencies.
    - `Sources/AutomationHealth/Views/ContentView.swift` contains no `FileManager`, `Data(contentsOf:)`, `Process`, or `/bin/launchctl`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains no `FileManager`, `Data(contentsOf:)`, `Process`, or `/bin/launchctl`.
  </acceptance_criteria>
  <done>The app target compiles with the extracted grouping helper and grouped sidebar rendering.</done>
</task>

<task type="auto">
  <name>Task 3: Run the existing local CI gate</name>
  <files>Sources/ActiveJobsCoreSelfTest/main.swift</files>
  <read_first>script/ci.sh, script/test.sh, Sources/ActiveJobsCoreSelfTest/main.swift</read_first>
  <action>Run `./script/ci.sh` from the repository root. If the new source-order self-test fails, fix the test or grouping-related source-order expectation. If an unrelated pre-existing failure appears, record the exact failing command and failure line in `01-03-SUMMARY.md` and do not mask it by weakening the new Phase 1 assertions.</action>
  <verify>./script/ci.sh</verify>
  <acceptance_criteria>
    - `./script/ci.sh` exits 0, or `01-03-SUMMARY.md` records an unrelated pre-existing failure with the exact failing command and failure line.
    - `Sources/ActiveJobsCoreSelfTest/main.swift` still prints `ActiveJobsCoreSelfTest passed` only after all top-level test calls complete.
  </acceptance_criteria>
  <done>The phase has either a passing local CI gate or a precise documented unrelated blocker for follow-up.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `swift run ActiveJobsCoreSelfTest` exits 0.
- [ ] `swift build` exits 0.
- [ ] `./script/ci.sh` exits 0, unless an unrelated pre-existing failure is documented in the summary.
- [ ] No new third-party dependencies are added.
- [ ] No scanner IO is added to SwiftUI views.
</verification>

<success_criteria>
- The stable source order contract is protected by the existing self-test executable.
- The extracted grouping helper and grouped sidebar view compile cleanly.
- The local CI workflow is run and its result is recorded.
- The phase remains read-only and dependency-free.
</success_criteria>

<output>
After completion, create `.planning/phases/01-sidebar-grouping-foundation/01-03-SUMMARY.md`.
</output>
