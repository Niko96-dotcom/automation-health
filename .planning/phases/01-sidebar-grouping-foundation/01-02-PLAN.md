---
phase: 01-sidebar-grouping-foundation
plan: 01-02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [Sources/AutomationHealth/Views/ContentView.swift, Sources/AutomationHealth/Views/SidebarView.swift]
autonomous: true
requirements: [ORG-01, ORG-02, ORG-03, ORG-05, SRCH-01]
user_setup: []
must_haves:
  truths:
    - "D-01: The rendered sidebar receives sections in the order produced by SidebarJobSection.sections(for:), which follows JobSource.allCases."
    - "D-02: SidebarView renders section.jobs in their existing order without sorting."
    - "D-03: SidebarView has no placeholder or header rendering path for empty source sections."
    - "D-04: Source headers render `section.title` and `section.visibleCount` in the format `{source name} ({visible count})`."
    - "D-05: SidebarJobRow displays source-free grouped subtitles generated by SidebarJobSummary."
    - "D-06: ContentView builds sidebar sections from `filteredJobs`, so active search results remain grouped with visible-only counts."
    - "D-07: SidebarHeader receives the summed visible row count from sections, matching search-filtered results."
  artifacts:
    - path: "Sources/AutomationHealth/Views/ContentView.swift"
      provides: "Filter-then-group sidebar data flow"
      contains: "SidebarJobSection.sections(for: filteredJobs)"
    - path: "Sources/AutomationHealth/Views/SidebarView.swift"
      provides: "Grouped source section rendering"
      contains: "SourceSectionHeader"
  key_links:
    - from: "Sources/AutomationHealth/Views/ContentView.swift"
      to: "Sources/AutomationHealth/Views/SidebarView.swift"
      via: "`sections:` parameter"
      pattern: "SidebarView\\(\\s*sections: sidebarSections"
---

<objective>
Render source section headers and grouped job rows in the sidebar.

Purpose: Make the visible sidebar easier to scan by grouping jobs under non-selectable source headers while preserving row selection behavior.
Output: `ContentView` passes grouped sections to `SidebarView`; `SidebarView` renders compact source headers and section rows.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-sidebar-grouping-foundation/01-CONTEXT.md
@.planning/phases/01-sidebar-grouping-foundation/01-RESEARCH.md
@.planning/phases/01-sidebar-grouping-foundation/01-UI-SPEC.md
@.planning/phases/01-sidebar-grouping-foundation/01-PATTERNS.md
@.planning/phases/01-sidebar-grouping-foundation/01-01-SUMMARY.md
@Sources/AutomationHealth/Views/ContentView.swift
@Sources/AutomationHealth/Views/SidebarView.swift
@Sources/AutomationHealth/Models/JobPresentation.swift
</context>

<threat_model>
Trust boundaries: This plan changes SwiftUI rendering over in-memory presentation data. It does not add scheduler writes, filesystem reads, network calls, persistence, or command execution.

STRIDE register:
- threat_id: P1-02-T1
  category: Tampering
  component: `SidebarView`
  disposition: mitigated
  mitigation_plan: Keep selection writes limited to existing job-row button actions that assign `selectedJobID = job.id`. Source headers must not mutate state.
- threat_id: P1-02-T2
  category: Information Disclosure
  component: source headers and subtitles
  disposition: mitigated
  mitigation_plan: Headers show only source display names and visible counts. Row subtitles show only existing timing/schedule text.
- threat_id: P1-02-T3
  category: Elevation of Privilege
  component: sidebar interactions
  disposition: mitigated
  mitigation_plan: Do not introduce job editing controls, context menus, launchctl actions, file writes, or scanner calls from views.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Pass grouped sections from ContentView</name>
  <files>Sources/AutomationHealth/Views/ContentView.swift</files>
  <read_first>Sources/AutomationHealth/Views/ContentView.swift, Sources/AutomationHealth/Models/JobPresentation.swift, .planning/phases/01-sidebar-grouping-foundation/01-CONTEXT.md, .planning/phases/01-sidebar-grouping-foundation/01-UI-SPEC.md</read_first>
  <action>Replace the current `private var sidebarJobs: [SidebarJobSummary]` computed property with `private var sidebarSections: [SidebarJobSection] { SidebarJobSection.sections(for: filteredJobs) }`. Update the `SidebarView` call to pass `sections: sidebarSections` instead of `jobs: sidebarJobs`. Keep `filteredJobs` unchanged so search filtering still trims and lowercases the search query before grouping. Do not move filtering into `SidebarView` and do not read scheduler files from `ContentView`.</action>
  <verify>swift build</verify>
  <acceptance_criteria>
    - `Sources/AutomationHealth/Views/ContentView.swift` contains `private var sidebarSections: [SidebarJobSection]`.
    - `Sources/AutomationHealth/Views/ContentView.swift` contains `SidebarJobSection.sections(for: filteredJobs)`.
    - `Sources/AutomationHealth/Views/ContentView.swift` contains `sections: sidebarSections`.
    - `Sources/AutomationHealth/Views/ContentView.swift` no longer contains `private var sidebarJobs`.
  </acceptance_criteria>
  <done>`ContentView` groups only the currently visible filtered jobs before rendering the sidebar.</done>
</task>

<task type="auto">
  <name>Task 2: Render source sections in SidebarView</name>
  <files>Sources/AutomationHealth/Views/SidebarView.swift</files>
  <read_first>Sources/AutomationHealth/Views/SidebarView.swift, Sources/AutomationHealth/Models/JobPresentation.swift, .planning/phases/01-sidebar-grouping-foundation/01-CONTEXT.md, .planning/phases/01-sidebar-grouping-foundation/01-UI-SPEC.md</read_first>
  <action>Change `SidebarView` to accept `let sections: [SidebarJobSection]` instead of `let jobs: [SidebarJobSummary]`. Add a private computed property `visibleJobs` that returns `sections.flatMap(\\.jobs)`. Update `statusSummary` and `SidebarHeader(jobCount:)` to use `visibleJobs`, so the top summary count reflects filtered visible rows. Inside the `LazyVStack`, replace the single `ForEach(jobs)` loop with `ForEach(sections) { section in SourceSectionHeader(section: section); ForEach(section.jobs) { job in SidebarJobRow(...) } }`. Do not add click handlers, buttons, focus state, or selection writes to source headers. Keep `SidebarJobRow` and `HealthDot` behavior unchanged except for consuming the already source-free subtitle.</action>
  <verify>swift build</verify>
  <acceptance_criteria>
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `let sections: [SidebarJobSection]`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `private var visibleJobs: [SidebarJobSummary]`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `sections.flatMap(\\.jobs)`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `ForEach(sections)`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `ForEach(section.jobs)`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` still contains `selectedJobID = job.id` only inside `SidebarJobRow` selection handling.
  </acceptance_criteria>
  <done>`SidebarView` renders non-empty source sections and keeps job rows as the only selectable elements.</done>
</task>

<task type="auto">
  <name>Task 3: Add compact source header styling</name>
  <files>Sources/AutomationHealth/Views/SidebarView.swift</files>
  <read_first>Sources/AutomationHealth/Views/SidebarView.swift, .planning/phases/01-sidebar-grouping-foundation/01-UI-SPEC.md</read_first>
  <action>Add a private SwiftUI view `SourceSectionHeader` in `SidebarView.swift` with `let section: SidebarJobSection`. Its body must render `Text("\\(section.title) (\\(section.visibleCount))")`, use `.font(.system(size: 11, weight: .semibold))`, `.foregroundStyle(.secondary)`, `.lineLimit(1)`, `.frame(maxWidth: .infinity, alignment: .leading)`, and compact padding using 8px horizontal padding plus 4px top/bottom spacing. Keep the header plain text/layout only, not a `Button` and not an accessibility focus target with custom selection behavior.</action>
  <verify>swift build</verify>
  <acceptance_criteria>
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `private struct SourceSectionHeader: View`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `Text("\\(section.title) (\\(section.visibleCount))")`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `.font(.system(size: 11, weight: .semibold))`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `.foregroundStyle(.secondary)`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains no `Button` inside `SourceSectionHeader`.
  </acceptance_criteria>
  <done>Source headers match the UI contract format and remain non-selectable structure.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `swift build` exits 0.
- [ ] `ContentView` passes `SidebarJobSection.sections(for: filteredJobs)` into `SidebarView`.
- [ ] `SidebarView` renders `ForEach(sections)` and `ForEach(section.jobs)`.
- [ ] `SourceSectionHeader` displays `"{source name} ({visible count})"`.
- [ ] No scanner or filesystem APIs are added to `ContentView.swift` or `SidebarView.swift`.
</verification>

<success_criteria>
- User sees visible jobs grouped under source section headers.
- Source headers show readable source names and visible counts.
- Source sections with no visible jobs do not render because the section model omits them.
- Search results keep the same source-grouped structure because grouping happens after filtering.
- Sidebar row selection remains bound only to job rows.
</success_criteria>

<output>
After completion, create `.planning/phases/01-sidebar-grouping-foundation/01-02-SUMMARY.md`.
</output>
