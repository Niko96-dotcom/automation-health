---
phase: 06-inventory-model-and-deterministic-sources
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/ActiveJobsCore/Models/ScheduledJob.swift
  - Sources/ActiveJobsCore/Services/JobScanning.swift
  - Sources/ActiveJobsCore/Services/LaunchAgentScanner.swift
  - Sources/ActiveJobsCore/Services/HermesCronScanner.swift
  - Sources/AutomationHealth/Stores/JobStore.swift
  - Sources/AutomationHealth/Models/JobPresentation.swift
  - Sources/AutomationHealth/Views/ContentView.swift
  - Sources/AutomationHealth/Views/SidebarView.swift
  - Sources/ActiveJobsCoreSelfTest/main.swift
  - docs/scanner-extension-guide.md
autonomous: true
requirements: [DISC-01, DISC-02, DISC-07, QUAL-01, QUAL-02]
user_setup: []
must_haves:
  truths:
    - "DISC-01: `JobConfidence` exists below the view layer with cases `scheduled`, `registered`, `candidate`, and `manual`."
    - "DISC-02: `JobOrigin` exists below the view layer with cases `userAuthored`, `thirdPartyApp`, `system`, and `unknown`, and it is stored independently from `JobSource`."
    - "DISC-07: source-specific scan notes are carried by `JobScanResult.notes` so a source can report limitations even when it returns no jobs."
    - "Existing launchd and Hermes cron jobs keep scheduled confidence after the model migration."
    - "Scanner IO remains in `ActiveJobsCore`; SwiftUI views consume `JobStore` and presentation values only."
    - "No scanner or view mutates scheduled jobs, scheduler files, scripts, Shortcuts, Automator workflows, or third-party automation systems."
  artifacts:
    - path: "Sources/ActiveJobsCore/Models/ScheduledJob.swift"
      provides: "Source-independent confidence, origin, scan-note, and scheduled job model fields"
    - path: "Sources/ActiveJobsCore/Services/JobScanning.swift"
      provides: "`JobScanResult` aggregation, job dedupe/sort, and scan-note preservation"
    - path: "Sources/AutomationHealth/Stores/JobStore.swift"
      provides: "Main-actor published scan notes beside job presentation data"
    - path: "Sources/AutomationHealth/Models/JobPresentation.swift"
      provides: "UI-ready confidence and origin display/search fields"
    - path: "Sources/ActiveJobsCoreSelfTest/main.swift"
      provides: "Fixture-driven tests for confidence, origin, and scan-note aggregation"
  key_links:
    - from: "JobScanning.refresh()"
      to: "JobStore.refresh()"
      via: "Inventory returns `JobScanResult`; store maps jobs to presentation rows and publishes notes"
      pattern: "JobScanResult"
    - from: "ScheduledJob"
      to: "JobPresentation"
      via: "Presentation fields expose `confidenceName` and `originName` without source-specific view logic"
      pattern: "confidenceName"
    - from: "docs/scanner-extension-guide.md"
      to: "JobScanning"
      via: "Contributor docs describe `JobScanResult` and scan-note contract"
      pattern: "JobScanResult"
---

<objective>
Extend the core inventory contract with source-independent confidence, origin, and scan-note concepts.

Purpose: Phase 6 needs a common model foundation before adding cron, Shortcuts, and Automator sources. Existing launchd and Hermes cron records should still behave as scheduled jobs, while new scanners can return registered records and source notes without leaking IO into SwiftUI views.

Output: Updated core model/protocol/store/presentation code, existing scanner migrations, and focused self-test coverage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-inventory-model-and-deterministic-sources/06-RESEARCH.md
@.planning/phases/06-inventory-model-and-deterministic-sources/06-VALIDATION.md
@.planning/phases/06-inventory-model-and-deterministic-sources/06-PATTERNS.md
@Sources/ActiveJobsCore/Models/ScheduledJob.swift
@Sources/ActiveJobsCore/Services/JobScanning.swift
@Sources/ActiveJobsCore/Services/LaunchAgentScanner.swift
@Sources/ActiveJobsCore/Services/HermesCronScanner.swift
@Sources/AutomationHealth/Stores/JobStore.swift
@Sources/AutomationHealth/Models/JobPresentation.swift
@Sources/AutomationHealth/Views/ContentView.swift
@Sources/AutomationHealth/Views/SidebarView.swift
@Sources/ActiveJobsCoreSelfTest/main.swift
@docs/scanner-extension-guide.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add source-independent inventory model fields and scan result contract</name>
  <files>Sources/ActiveJobsCore/Models/ScheduledJob.swift, Sources/ActiveJobsCore/Services/JobScanning.swift</files>
  <read_first>Sources/ActiveJobsCore/Models/ScheduledJob.swift, Sources/ActiveJobsCore/Services/JobScanning.swift, .planning/phases/06-inventory-model-and-deterministic-sources/06-RESEARCH.md, .planning/phases/06-inventory-model-and-deterministic-sources/06-PATTERNS.md</read_first>
  <action>Update `Sources/ActiveJobsCore/Models/ScheduledJob.swift`. Add `public enum JobConfidence: String, Codable, CaseIterable, Hashable, Identifiable, Sendable` with exactly these cases in this order: `scheduled`, `registered`, `candidate`, `manual`. Add `id`, `displayName`, and `description` where display names are `Scheduled`, `Registered`, `Candidate`, and `Manual`; descriptions must include the exact phrases `Direct schedule evidence`, `Registered automation without schedule evidence`, `Possible automation candidate`, and `User-added app record`. Add `public enum JobOrigin: String, Codable, CaseIterable, Hashable, Identifiable, Sendable` with exactly these cases in this order: `userAuthored`, `thirdPartyApp`, `system`, `unknown`. Add `id` and `displayName` where display names are `User-authored`, `Third-party app`, `System`, and `Unknown`. Add `public enum ScanNoteSeverity: String, Codable, CaseIterable, Hashable, Identifiable, Sendable` with cases `info`, `warning`, `error` and display names `Info`, `Warning`, `Error`. Add `public struct ScanNote: Identifiable, Hashable, Sendable` with `public let id: String`, `source: JobSource`, `severity: ScanNoteSeverity`, `message: String`, and `detail: String?`, plus a public initializer whose default `id` is deterministic from `source.rawValue`, `severity.rawValue`, `message`, and `detail ?? ""` joined by `:`. Add `public let confidence: JobConfidence` and `public let origin: JobOrigin` to `ScheduledJob`, and add defaulted initializer parameters `confidence: JobConfidence = .scheduled` and `origin: JobOrigin = .unknown` after `source`. Update assignments accordingly. Update `Sources/ActiveJobsCore/Services/JobScanning.swift`: add `public struct JobScanResult: Hashable, Sendable` with `public let jobs: [ScheduledJob]`, `public let notes: [ScanNote]`, and public initializer defaulting both to empty arrays. Change `JobScanning.scan()` to return `JobScanResult`. Change `JobInventory.refresh()` to return `JobScanResult`, collect each scanner result, dedupe/sort jobs exactly as before, and return `JobScanResult(jobs: uniqueSortedJobs, notes: results.flatMap(\\.notes))`.</action>
  <verify>
    <automated>rg -n "enum JobConfidence|case scheduled|case registered|case candidate|case manual|enum JobOrigin|case userAuthored|case thirdPartyApp|case system|case unknown|enum ScanNoteSeverity|struct ScanNote|confidence: JobConfidence = \\.scheduled|origin: JobOrigin = \\.unknown" Sources/ActiveJobsCore/Models/ScheduledJob.swift && rg -n "struct JobScanResult|func scan\\(\\) throws -> JobScanResult|func refresh\\(\\) throws -> JobScanResult|flatMap\\(\\\\.notes\\)" Sources/ActiveJobsCore/Services/JobScanning.swift</automated>
  </verify>
  <acceptance_criteria>
    - `ScheduledJob.swift` contains `enum JobConfidence`.
    - `ScheduledJob.swift` contains `case scheduled`, `case registered`, `case candidate`, and `case manual`.
    - `ScheduledJob.swift` contains `enum JobOrigin`.
    - `ScheduledJob.swift` contains `case userAuthored`, `case thirdPartyApp`, `case system`, and `case unknown`.
    - `ScheduledJob.swift` contains `struct ScanNote`.
    - `ScheduledJob.swift` contains `confidence: JobConfidence = .scheduled`.
    - `ScheduledJob.swift` contains `origin: JobOrigin = .unknown`.
    - `JobScanning.swift` contains `struct JobScanResult`.
    - `JobScanning.swift` contains `func scan() throws -> JobScanResult`.
    - `JobScanning.swift` contains `func refresh() throws -> JobScanResult`.
    - `JobScanning.swift` contains `flatMap(\\.notes)`.
  </acceptance_criteria>
  <done>The core model can represent confidence, origin, and source notes independently from scanner source.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate existing scanners and inventory tests to the new result contract</name>
  <files>Sources/ActiveJobsCore/Services/LaunchAgentScanner.swift, Sources/ActiveJobsCore/Services/HermesCronScanner.swift, Sources/ActiveJobsCoreSelfTest/main.swift</files>
  <read_first>Sources/ActiveJobsCore/Services/LaunchAgentScanner.swift, Sources/ActiveJobsCore/Services/HermesCronScanner.swift, Sources/ActiveJobsCore/Services/JobScanning.swift, Sources/ActiveJobsCore/Models/ScheduledJob.swift, Sources/ActiveJobsCoreSelfTest/main.swift, .planning/phases/06-inventory-model-and-deterministic-sources/06-RESEARCH.md</read_first>
  <action>Update `LaunchAgentScanner.scan()` and `HermesCronScanner.scan()` to return `JobScanResult`. Keep their existing parse/filter behavior, but wrap found jobs in `JobScanResult(jobs: jobs, notes: [])`. For each existing `ScheduledJob` returned by `LaunchAgentScanner`, pass `confidence: .scheduled` and a derived `origin`. Add a private launchd origin helper with these exact outcomes: labels starting with `com.apple.` or commands starting with `/System/`, `/usr/libexec/`, `/bin/`, `/sbin/`, `/usr/bin/`, or `/usr/sbin/` return `.system`; plist paths under `homeDirectory.path` return `.userAuthored`; commands containing `.app/Contents/MacOS/` or starting with `/Applications/` or `/Library/Application Support/` return `.thirdPartyApp`; otherwise return `.unknown`. For `HermesCronScanner`, pass `confidence: .scheduled` and `origin: .userAuthored`. Update all self-test scanner calls from `.scan()` to `.scan().jobs` where they need jobs, and update `JobInventory.refresh()` callers from arrays to `.jobs`. Add tests named `testJobConfidenceAndOriginCases()`, `testInventoryAggregatesJobsAndScanNotes()`, and `testExistingScannersPopulateScheduledConfidenceAndOrigins()`.</action>
  <verify>
    <automated>rg -n "func scan\\(\\) throws -> JobScanResult|JobScanResult\\(jobs:|confidence: \\.scheduled|origin: \\.userAuthored|launchdOrigin" Sources/ActiveJobsCore/Services/LaunchAgentScanner.swift Sources/ActiveJobsCore/Services/HermesCronScanner.swift && rg -n "testJobConfidenceAndOriginCases|testInventoryAggregatesJobsAndScanNotes|testExistingScannersPopulateScheduledConfidenceAndOrigins|\\.scan\\(\\)\\.jobs|\\.refresh\\(\\)\\.jobs" Sources/ActiveJobsCoreSelfTest/main.swift</automated>
  </verify>
  <acceptance_criteria>
    - `LaunchAgentScanner.swift` contains `func scan() throws -> JobScanResult`.
    - `LaunchAgentScanner.swift` contains `JobScanResult(jobs:`.
    - `LaunchAgentScanner.swift` contains `confidence: .scheduled`.
    - `LaunchAgentScanner.swift` contains `launchdOrigin`.
    - `HermesCronScanner.swift` contains `func scan() throws -> JobScanResult`.
    - `HermesCronScanner.swift` contains `origin: .userAuthored`.
    - `Sources/ActiveJobsCoreSelfTest/main.swift` contains `testJobConfidenceAndOriginCases`.
    - `Sources/ActiveJobsCoreSelfTest/main.swift` contains `testInventoryAggregatesJobsAndScanNotes`.
    - `Sources/ActiveJobsCoreSelfTest/main.swift` contains `testExistingScannersPopulateScheduledConfidenceAndOrigins`.
    - `./script/test.sh` exits 0.
  </acceptance_criteria>
  <done>Existing scanners compile against `JobScanResult` and preserve scheduled job behavior with explicit confidence/origin values.</done>
</task>

<task type="auto">
  <name>Task 3: Expose confidence, origin, and scan notes through store and presentation data</name>
  <files>Sources/AutomationHealth/Stores/JobStore.swift, Sources/AutomationHealth/Models/JobPresentation.swift, Sources/AutomationHealth/Views/ContentView.swift, Sources/AutomationHealth/Views/SidebarView.swift</files>
  <read_first>Sources/AutomationHealth/Stores/JobStore.swift, Sources/AutomationHealth/Models/JobPresentation.swift, Sources/AutomationHealth/Views/ContentView.swift, Sources/AutomationHealth/Views/SidebarView.swift, Sources/ActiveJobsCore/Models/ScheduledJob.swift, Sources/ActiveJobsCore/Services/JobScanning.swift</read_first>
  <action>Update `JobStore` with `@Published private(set) var scanNotes: [ScanNote] = []`. In `refresh()`, call the new `inventory.refresh()` result once inside the detached task, map `result.jobs` into `[JobPresentation]`, and return both presentations and `result.notes` to the main actor. On success, assign both `jobs = refreshed.jobs` and `scanNotes = refreshed.notes`. On failure, preserve the existing `errorMessage` behavior and set `scanNotes` to an array containing one `ScanNote(source: .launchd, severity: .error, message: "Scan failed", detail: error.localizedDescription)` only if no source-specific result exists. Update `JobPresentation` with `let confidenceName: String` and `let originName: String`, assigned from `job.confidence.displayName` and `job.origin.displayName`. Add both display names to `searchText`. Update `ContentView` to pass `store.scanNotes` into `SidebarView`. Update `SidebarView` initializer with `let scanNotes: [ScanNote]`. In the existing footer, keep `lastScannedDescription` visible, and when `scanNotes` is not empty append ` • \(scanNotes.count) scan note` for one note or ` • \(scanNotes.count) scan notes` for more than one. Add `.help(scanNotes.map { $0.message }.joined(separator: "\\n"))` to the footer text or parent footer row so users can inspect note messages without scanner IO in the view.</action>
  <verify>
    <automated>rg -n "scanNotes: \\[ScanNote\\]|result\\.jobs|result\\.notes|confidenceName|originName" Sources/AutomationHealth/Stores/JobStore.swift Sources/AutomationHealth/Models/JobPresentation.swift && rg -n "scanNotes|scan note|store\\.scanNotes|\\.help\\(" Sources/AutomationHealth/Views/ContentView.swift Sources/AutomationHealth/Views/SidebarView.swift</automated>
  </verify>
  <acceptance_criteria>
    - `JobStore.swift` contains `scanNotes: [ScanNote]`.
    - `JobStore.swift` contains `result.jobs`.
    - `JobStore.swift` contains `result.notes`.
    - `JobPresentation.swift` contains `confidenceName`.
    - `JobPresentation.swift` contains `originName`.
    - `JobPresentation.swift` search text includes confidence and origin display names.
    - `ContentView.swift` contains `store.scanNotes`.
    - `SidebarView.swift` contains `scanNotes`.
    - `SidebarView.swift` contains `scan note`.
    - `SidebarView.swift` contains `.help(` for scan-note messages.
    - No SwiftUI view contains `crontab`, `shortcuts list`, `.workflow`, `Data(contentsOf:)`, or `FileManager.default.contentsOfDirectory`.
  </acceptance_criteria>
  <done>The app layer can display source-note summaries and search confidence/origin fields without direct scheduler IO in views.</done>
</task>

<task type="auto">
  <name>Task 4: Document the expanded scanner contract and run the full gate</name>
  <files>docs/scanner-extension-guide.md, Sources/ActiveJobsCoreSelfTest/main.swift</files>
  <read_first>docs/scanner-extension-guide.md, Sources/ActiveJobsCoreSelfTest/main.swift, Sources/ActiveJobsCore/Models/ScheduledJob.swift, Sources/ActiveJobsCore/Services/JobScanning.swift, .planning/phases/06-inventory-model-and-deterministic-sources/06-VALIDATION.md</read_first>
  <action>Update `docs/scanner-extension-guide.md` so the Core Contracts section mentions `JobScanResult`, `JobConfidence`, `JobOrigin`, and `ScanNote`. Add exact guidance that scanners should return `JobScanResult(jobs:notes:)`, use `.scheduled` only for direct schedule evidence, use `.registered` for known automations without schedule evidence, use `.candidate` for possible automations, use `.manual` only for app-authored manual records, and report non-fatal source limitations as scan notes. Add a sentence that SwiftUI views must not read scheduler files or execute scanner commands. Ensure `Sources/ActiveJobsCoreSelfTest/main.swift` calls the three new tests from Task 2 near the top-level test list. Run `./script/test.sh` and `./script/ci.sh`.</action>
  <verify>
    <automated>rg -n "JobScanResult|JobConfidence|JobOrigin|ScanNote|scheduled.*direct schedule evidence|registered.*without schedule evidence|candidate|manual|SwiftUI views must not" docs/scanner-extension-guide.md && rg -n "try testJobConfidenceAndOriginCases\\(\\)|try testInventoryAggregatesJobsAndScanNotes\\(\\)|try testExistingScannersPopulateScheduledConfidenceAndOrigins\\(\\)" Sources/ActiveJobsCoreSelfTest/main.swift && ./script/ci.sh</automated>
  </verify>
  <acceptance_criteria>
    - `docs/scanner-extension-guide.md` contains `JobScanResult`.
    - `docs/scanner-extension-guide.md` contains `JobConfidence`.
    - `docs/scanner-extension-guide.md` contains `JobOrigin`.
    - `docs/scanner-extension-guide.md` contains `ScanNote`.
    - `docs/scanner-extension-guide.md` contains `direct schedule evidence`.
    - `docs/scanner-extension-guide.md` contains `without schedule evidence`.
    - `Sources/ActiveJobsCoreSelfTest/main.swift` contains `try testJobConfidenceAndOriginCases()`.
    - `Sources/ActiveJobsCoreSelfTest/main.swift` contains `try testInventoryAggregatesJobsAndScanNotes()`.
    - `Sources/ActiveJobsCoreSelfTest/main.swift` contains `try testExistingScannersPopulateScheduledConfidenceAndOrigins()`.
    - `./script/ci.sh` exits 0.
  </acceptance_criteria>
  <done>The scanner extension docs and self-tests verify the new model/result contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scanner source data -> normalized model | Source-specific evidence is converted into confidence, origin, jobs, and notes. |
| Core scanner result -> SwiftUI store | Scanner IO completes before view state is published. |
| Missing or unreadable source -> user-facing note | Best-effort limitations are represented without throwing away all inventory data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Spoofing | Confidence/origin labels | mitigate | Use explicit enums and conservative derivation; default to `.unknown` origin rather than overclaiming ownership. |
| T-06-01-02 | Tampering | Scanner result contract | mitigate | Preserve read-only scanner behavior and keep mutation commands out of model/store/view code. |
| T-06-01-03 | Information Disclosure | Scan-note display | mitigate | Display only scanner limitation messages, not raw private scheduler output, paths beyond existing detail paths, or command stdout. |
| T-06-01-04 | Denial of Service | Inventory aggregation | mitigate | Preserve existing dedupe/sort behavior and aggregate notes without changing scanner execution order or adding live recursive scans. |
| T-06-01-05 | Elevation of Privilege | View-layer scanner IO | avoid | Do not add filesystem reads, process execution, or scheduler-specific branches to SwiftUI views. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `rg -n "enum JobConfidence|enum JobOrigin|struct ScanNote" Sources/ActiveJobsCore/Models/ScheduledJob.swift`
- [ ] `rg -n "struct JobScanResult|func scan\\(\\) throws -> JobScanResult|func refresh\\(\\) throws -> JobScanResult" Sources/ActiveJobsCore/Services/JobScanning.swift`
- [ ] `rg -n "confidence: \\.scheduled|origin: \\.userAuthored|JobScanResult\\(jobs:" Sources/ActiveJobsCore/Services/LaunchAgentScanner.swift Sources/ActiveJobsCore/Services/HermesCronScanner.swift`
- [ ] `rg -n "scanNotes: \\[ScanNote\\]|result\\.jobs|result\\.notes" Sources/AutomationHealth/Stores/JobStore.swift`
- [ ] `rg -n "confidenceName|originName" Sources/AutomationHealth/Models/JobPresentation.swift`
- [ ] `rg -n "scanNotes|scan note" Sources/AutomationHealth/Views/ContentView.swift Sources/AutomationHealth/Views/SidebarView.swift`
- [ ] `rg -n "JobScanResult|JobConfidence|JobOrigin|ScanNote" docs/scanner-extension-guide.md`
- [ ] `./script/ci.sh`
</verification>

<success_criteria>
- DISC-01 is covered because `JobConfidence` exists below the view layer with Scheduled, Registered, Candidate, and Manual states.
- DISC-02 is covered because `JobOrigin` exists below the view layer and is stored independently from `JobSource`.
- DISC-07 is covered because scanners can return source-specific `ScanNote` values through `JobScanResult.notes`.
- QUAL-01 is preserved because this plan adds no scheduler mutation commands and no job management UI.
- QUAL-02 is covered by executable self-tests that exercise the model/result contract with synthetic fixtures.
- Existing launchd, Hermes cron, search, selection, refresh, and CI behavior continue to compile and pass `./script/ci.sh`.
</success_criteria>

<output>
After completion, create `.planning/phases/06-inventory-model-and-deterministic-sources/06-01-SUMMARY.md`.
</output>
