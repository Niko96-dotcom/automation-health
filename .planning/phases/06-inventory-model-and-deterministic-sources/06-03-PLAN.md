---
phase: 06-inventory-model-and-deterministic-sources
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - Sources/ActiveJobsCore/Models/ScheduledJob.swift
  - Sources/ActiveJobsCore/Services/JobScanning.swift
  - Sources/ActiveJobsCore/Services/ShortcutsScanner.swift
  - Sources/ActiveJobsCore/Services/AutomatorScanner.swift
  - Sources/ActiveJobsCoreSelfTest/main.swift
  - docs/scheduled-job-sources.md
  - README.md
autonomous: true
requirements: [DISC-04, DISC-05, DISC-07, QUAL-01, QUAL-02]
user_setup: []
must_haves:
  truths:
    - "DISC-04: Shortcuts inventory uses `/usr/bin/shortcuts list --show-identifiers` when available and never treats listed shortcuts as proven scheduled jobs."
    - "DISC-05: Automator workflow inventory scans bounded readable workflow locations and labels records according to available evidence."
    - "DISC-07: missing Shortcuts command, Shortcuts command failures, missing Automator directories, and unreadable workflow locations are reported as source-specific scan notes."
    - "QUAL-01: scanners never run, view, sign, edit, or execute Shortcuts or Automator workflows."
    - "QUAL-02: scanners are tested with injected command runners and temporary fixture directories, not live local Shortcuts or workflow state."
    - "Shortcuts and Automator records use `JobConfidence.registered` unless direct schedule evidence is added by a later scanner."
  artifacts:
    - path: "Sources/ActiveJobsCore/Services/ShortcutsScanner.swift"
      provides: "Registered Shortcuts inventory through injected list command runner"
    - path: "Sources/ActiveJobsCore/Services/AutomatorScanner.swift"
      provides: "Bounded Automator workflow inventory through injected directories"
    - path: "Sources/ActiveJobsCore/Models/ScheduledJob.swift"
      provides: "`JobSource.shortcuts` and `JobSource.automator` display and ordering"
    - path: "Sources/ActiveJobsCore/Services/JobScanning.swift"
      provides: "Live inventory composition including Shortcuts and Automator scanners"
    - path: "Sources/ActiveJobsCoreSelfTest/main.swift"
      provides: "Synthetic Shortcuts and Automator scanner tests"
    - path: "docs/scheduled-job-sources.md"
      provides: "Registered-source documentation and evidence limits"
  key_links:
    - from: "ShortcutsScanner"
      to: "JobInventory.live"
      via: "Live composition includes registered Shortcuts source after cron"
      pattern: "ShortcutsScanner()"
    - from: "AutomatorScanner"
      to: "JobInventory.live"
      via: "Live composition includes Automator source after Shortcuts"
      pattern: "AutomatorScanner(homeDirectory: homeDirectory)"
    - from: "ShortcutsScanner and AutomatorScanner"
      to: "docs/scheduled-job-sources.md"
      via: "Docs state registered confidence and no schedule evidence"
      pattern: "Registered confidence"
---

<objective>
Add registered Shortcuts and Automator inventory scanners with conservative confidence and source-note behavior.

Purpose: Phase 6 should broaden inventory to known local automation systems without overclaiming scheduling evidence or mutating user automations.

Output: `ShortcutsScanner`, `AutomatorScanner`, source enum/composition updates, self-tests, and source docs.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-inventory-model-and-deterministic-sources/06-RESEARCH.md
@.planning/phases/06-inventory-model-and-deterministic-sources/06-VALIDATION.md
@.planning/phases/06-inventory-model-and-deterministic-sources/06-PATTERNS.md
@Sources/ActiveJobsCore/Models/ScheduledJob.swift
@Sources/ActiveJobsCore/Services/JobScanning.swift
@Sources/ActiveJobsCore/Services/CronScanner.swift
@Sources/ActiveJobsCoreSelfTest/main.swift
@docs/scheduled-job-sources.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add registered Shortcuts scanner with injected command runner</name>
  <files>Sources/ActiveJobsCore/Models/ScheduledJob.swift, Sources/ActiveJobsCore/Services/ShortcutsScanner.swift</files>
  <read_first>Sources/ActiveJobsCore/Models/ScheduledJob.swift, Sources/ActiveJobsCore/Services/CronScanner.swift, .planning/phases/06-inventory-model-and-deterministic-sources/06-RESEARCH.md, .planning/phases/06-inventory-model-and-deterministic-sources/06-PATTERNS.md</read_first>
  <action>Update `JobSource` in `ScheduledJob.swift` by adding `case shortcuts` after `cron`, with display name `Shortcuts`. Create `Sources/ActiveJobsCore/Services/ShortcutsScanner.swift`. Define `public struct ShortcutsScanner: JobScanning, Sendable` with injected `commandRunner: ShortcutsCommandRunning`, defaulting to `ProcessShortcutsCommandRunner()`. Define `public protocol ShortcutsCommandRunning: Sendable` with `func listShortcuts() -> ShortcutsCommandResult`. Define `public struct ShortcutsCommandResult: Sendable, Hashable` with `output: String`, `errorOutput: String`, and `exitCode: Int32`, plus public initializer. Production `ProcessShortcutsCommandRunner` must invoke `/usr/bin/shortcuts` with exactly `["list", "--show-identifiers"]`; it must not invoke `run`, `view`, or `sign`. `ShortcutsScanner.scan()` must return `JobScanResult`. When `exitCode == 0`, parse each non-empty output line into a shortcut name and optional identifier by extracting a final parenthesized token as the identifier, for example `Morning Routine (ABC-123)` becomes name `Morning Routine` and id `ABC-123`; if no identifier exists, derive a deterministic id slug from the name. For each shortcut, return a `ScheduledJob` with `source: .shortcuts`, `confidence: .registered`, `origin: .userAuthored`, `schedule: "Registered shortcut (no schedule evidence)"`, `command: nil`, `state: "registered"`, `lastStatus: nil`, `lastRun: nil`, `nextRun: nil`, `definition` containing the shortcut name and identifier when present, and no detail path. When `exitCode != 0`, return no jobs and one warning scan note whose message contains `Shortcuts list failed` and whose detail contains the command stderr or exit code.</action>
  <verify>
    <automated>test -f Sources/ActiveJobsCore/Services/ShortcutsScanner.swift && rg -n "case shortcuts|Shortcuts" Sources/ActiveJobsCore/Models/ScheduledJob.swift && rg -n "struct ShortcutsScanner|protocol ShortcutsCommandRunning|struct ShortcutsCommandResult|/usr/bin/shortcuts|\\[\"list\", \"--show-identifiers\"\\]|source: \\.shortcuts|confidence: \\.registered|Registered shortcut \\(no schedule evidence\\)|Shortcuts list failed" Sources/ActiveJobsCore/Services/ShortcutsScanner.swift && ! rg -n "shortcuts.*run|shortcuts.*view|shortcuts.*sign" Sources/ActiveJobsCore/Services/ShortcutsScanner.swift</automated>
  </verify>
  <acceptance_criteria>
    - `ScheduledJob.swift` contains `case shortcuts`.
    - `ShortcutsScanner.swift` exists.
    - `ShortcutsScanner.swift` contains `struct ShortcutsScanner`.
    - `ShortcutsScanner.swift` contains `protocol ShortcutsCommandRunning`.
    - `ShortcutsScanner.swift` contains `struct ShortcutsCommandResult`.
    - `ShortcutsScanner.swift` contains `/usr/bin/shortcuts`.
    - `ShortcutsScanner.swift` contains `["list", "--show-identifiers"]`.
    - `ShortcutsScanner.swift` contains `source: .shortcuts`.
    - `ShortcutsScanner.swift` contains `confidence: .registered`.
    - `ShortcutsScanner.swift` contains `Registered shortcut (no schedule evidence)`.
    - `ShortcutsScanner.swift` contains `Shortcuts list failed`.
    - `ShortcutsScanner.swift` does not contain `shortcuts` with `run`, `view`, or `sign` command arguments.
  </acceptance_criteria>
  <done>Shortcuts inventory is read-only, registered-confidence, and command-runner testable.</done>
</task>

<task type="auto">
  <name>Task 2: Add bounded Automator workflow scanner</name>
  <files>Sources/ActiveJobsCore/Models/ScheduledJob.swift, Sources/ActiveJobsCore/Services/AutomatorScanner.swift</files>
  <read_first>Sources/ActiveJobsCore/Models/ScheduledJob.swift, Sources/ActiveJobsCore/Services/LaunchAgentScanner.swift, Sources/ActiveJobsCore/Services/ShortcutsScanner.swift, .planning/phases/06-inventory-model-and-deterministic-sources/06-RESEARCH.md, .planning/phases/06-inventory-model-and-deterministic-sources/06-PATTERNS.md</read_first>
  <action>Update `JobSource` in `ScheduledJob.swift` by adding `case automator` after `shortcuts`, with display name `Automator`. Create `Sources/ActiveJobsCore/Services/AutomatorScanner.swift`. Define `public struct AutomatorScanner: JobScanning, @unchecked Sendable` with injected `homeDirectory: URL`, `workflowDirectories: [URL]?`, and `fileManager: FileManager`. Defaults must use `FileManager.default.homeDirectoryForCurrentUser`, `nil`, and `.default`; when `workflowDirectories` is nil, scan exactly `homeDirectory.appending(path: "Library/Services")` and `homeDirectory.appending(path: "Library/Workflows")`. Enumerate only these injected directories and their direct children; do not recursively scan arbitrary home-directory paths. Include workflow artifacts whose path extension is `workflow` or whose path extension is `app` and whose `Contents/Info.plist` contains an Automator-looking bundle name or package metadata. For each `.workflow` fixture, read `Contents/Info.plist` if present and prefer `CFBundleDisplayName`, then `CFBundleName`, then the bundle filename without extension. Return `ScheduledJob` with `source: .automator`, `confidence: .registered`, `origin: .userAuthored` when the workflow path is under `homeDirectory.path`, otherwise `.unknown`, `schedule: "Automator workflow (no schedule evidence)"`, `command: nil`, `state: "registered"`, nil run fields, `definition` containing the workflow path, and `detailPath` equal to the workflow path. Missing workflow directories must produce info scan notes; unreadable directories or plist parse failures must produce warning notes but not fail the whole scan.</action>
  <verify>
    <automated>test -f Sources/ActiveJobsCore/Services/AutomatorScanner.swift && rg -n "case automator|Automator" Sources/ActiveJobsCore/Models/ScheduledJob.swift && rg -n "struct AutomatorScanner|Library/Services|Library/Workflows|pathExtension == \"workflow\"|CFBundleDisplayName|CFBundleName|source: \\.automator|confidence: \\.registered|Automator workflow \\(no schedule evidence\\)|detailPath:" Sources/ActiveJobsCore/Services/AutomatorScanner.swift</automated>
  </verify>
  <acceptance_criteria>
    - `ScheduledJob.swift` contains `case automator`.
    - `AutomatorScanner.swift` exists.
    - `AutomatorScanner.swift` contains `struct AutomatorScanner`.
    - `AutomatorScanner.swift` contains `Library/Services`.
    - `AutomatorScanner.swift` contains `Library/Workflows`.
    - `AutomatorScanner.swift` contains `pathExtension == "workflow"`.
    - `AutomatorScanner.swift` contains `CFBundleDisplayName`.
    - `AutomatorScanner.swift` contains `CFBundleName`.
    - `AutomatorScanner.swift` contains `source: .automator`.
    - `AutomatorScanner.swift` contains `confidence: .registered`.
    - `AutomatorScanner.swift` contains `Automator workflow (no schedule evidence)`.
  </acceptance_criteria>
  <done>Automator workflow inventory is bounded, read-only, and conservative about schedule evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Compose registered scanners and add fixture-driven tests</name>
  <files>Sources/ActiveJobsCore/Services/JobScanning.swift, Sources/ActiveJobsCoreSelfTest/main.swift</files>
  <read_first>Sources/ActiveJobsCore/Services/JobScanning.swift, Sources/ActiveJobsCore/Services/ShortcutsScanner.swift, Sources/ActiveJobsCore/Services/AutomatorScanner.swift, Sources/ActiveJobsCore/Services/CronScanner.swift, Sources/ActiveJobsCoreSelfTest/main.swift, .planning/phases/06-inventory-model-and-deterministic-sources/06-VALIDATION.md</read_first>
  <action>Update `JobInventory.live(homeDirectory:)` so the scanner array is exactly `LaunchAgentScanner(homeDirectory: homeDirectory)`, `HermesCronScanner(homeDirectory: homeDirectory)`, `CronScanner()`, `ShortcutsScanner()`, and `AutomatorScanner(homeDirectory: homeDirectory)`, in that order. Update `testJobSourceOrderMatchesSidebarGroupingContract()` so `JobSource.allCases` equals `[.launchd, .hermesCron, .cron, .shortcuts, .automator]` and display names equal `["launchd", "Hermes cron", "cron", "Shortcuts", "Automator"]`. Add top-level calls and implementations for `testShortcutsScannerListsRegisteredShortcutsWithoutSchedulingEvidence()`, `testShortcutsScannerReportsCommandFailureAsScanNote()`, `testAutomatorScannerListsWorkflowFixturesAsRegistered()`, and `testAutomatorScannerReportsMissingWorkflowDirectoriesAsNotes()`. Add `StubShortcutsCommandRunner: ShortcutsCommandRunning`. The Shortcuts success fixture must return output containing `Morning Routine (ABC-123)` and assert source `.shortcuts`, confidence `.registered`, origin `.userAuthored`, state `registered`, schedule `Registered shortcut (no schedule evidence)`, nil `nextRun`, and definition containing `ABC-123`. The Shortcuts failure fixture must use `ShortcutsCommandResult(output: "", errorOutput: "shortcuts unavailable", exitCode: 69)` and assert a scan note containing `Shortcuts list failed`. The Automator fixture must create `Library/Services/Resize Images.workflow/Contents/Info.plist` with `CFBundleName` or `CFBundleDisplayName` of `Resize Images`, assert source `.automator`, confidence `.registered`, origin `.userAuthored`, state `registered`, schedule `Automator workflow (no schedule evidence)`, and `detailPath` ending in `.workflow`. The missing-directory test must use injected absent directories and assert at least one info scan note.</action>
  <verify>
    <automated>rg -n "CronScanner\\(\\)|ShortcutsScanner\\(\\)|AutomatorScanner\\(homeDirectory: homeDirectory\\)" Sources/ActiveJobsCore/Services/JobScanning.swift && rg -n "JobSource\\.allCases == \\[\\.launchd, \\.hermesCron, \\.cron, \\.shortcuts, \\.automator\\]|\\[\"launchd\", \"Hermes cron\", \"cron\", \"Shortcuts\", \"Automator\"\\]|testShortcutsScannerListsRegisteredShortcutsWithoutSchedulingEvidence|testShortcutsScannerReportsCommandFailureAsScanNote|testAutomatorScannerListsWorkflowFixturesAsRegistered|testAutomatorScannerReportsMissingWorkflowDirectoriesAsNotes|StubShortcutsCommandRunner|Morning Routine|Resize Images|shortcuts unavailable" Sources/ActiveJobsCoreSelfTest/main.swift && ./script/test.sh</automated>
  </verify>
  <acceptance_criteria>
    - `JobScanning.swift` contains `ShortcutsScanner()`.
    - `JobScanning.swift` contains `AutomatorScanner(homeDirectory: homeDirectory)`.
    - `Sources/ActiveJobsCoreSelfTest/main.swift` contains `JobSource.allCases == [.launchd, .hermesCron, .cron, .shortcuts, .automator]`.
    - `Sources/ActiveJobsCoreSelfTest/main.swift` contains `testShortcutsScannerListsRegisteredShortcutsWithoutSchedulingEvidence`.
    - `Sources/ActiveJobsCoreSelfTest/main.swift` contains `testShortcutsScannerReportsCommandFailureAsScanNote`.
    - `Sources/ActiveJobsCoreSelfTest/main.swift` contains `testAutomatorScannerListsWorkflowFixturesAsRegistered`.
    - `Sources/ActiveJobsCoreSelfTest/main.swift` contains `testAutomatorScannerReportsMissingWorkflowDirectoriesAsNotes`.
    - `Sources/ActiveJobsCoreSelfTest/main.swift` contains `Morning Routine`.
    - `Sources/ActiveJobsCoreSelfTest/main.swift` contains `Resize Images`.
    - `./script/test.sh` exits 0.
  </acceptance_criteria>
  <done>Live inventory includes registered Shortcuts and Automator sources with deterministic self-test coverage.</done>
</task>

<task type="auto">
  <name>Task 4: Document registered inventory limits and run full CI</name>
  <files>docs/scheduled-job-sources.md, README.md</files>
  <read_first>docs/scheduled-job-sources.md, README.md, Sources/ActiveJobsCore/Services/ShortcutsScanner.swift, Sources/ActiveJobsCore/Services/AutomatorScanner.swift, .planning/phases/06-inventory-model-and-deterministic-sources/06-RESEARCH.md</read_first>
  <action>Update `docs/scheduled-job-sources.md` with sections named `Shortcuts` and `Automator Workflows`. The Shortcuts section must state that the scanner invokes `/usr/bin/shortcuts list --show-identifiers`, records listed shortcuts as Registered confidence, does not claim schedule evidence, does not call `shortcuts run`, `shortcuts view`, or `shortcuts sign`, and reports missing command/permission failures as scan notes. The Automator section must state that the scanner reads bounded user workflow locations such as `~/Library/Services` and `~/Library/Workflows`, records readable workflows as Registered confidence, does not claim schedule evidence, does not execute workflows, and reports missing/unreadable locations as scan notes. Update README `What It Scans` to mention registered Shortcuts and Automator workflows. Update README `Known Limitations` so it says Shortcuts and Automator records are registered inventory, not proof of scheduling. Run `./script/ci.sh`.</action>
  <verify>
    <automated>rg -n "Shortcuts|/usr/bin/shortcuts list --show-identifiers|Registered confidence|does not claim schedule evidence|shortcuts run|scan notes|Automator Workflows|~/Library/Services|~/Library/Workflows|does not execute workflows" docs/scheduled-job-sources.md && rg -n "registered Shortcuts|Automator workflows|registered inventory, not proof of scheduling" README.md && ./script/ci.sh</automated>
  </verify>
  <acceptance_criteria>
    - `docs/scheduled-job-sources.md` contains `Shortcuts`.
    - `docs/scheduled-job-sources.md` contains `/usr/bin/shortcuts list --show-identifiers`.
    - `docs/scheduled-job-sources.md` contains `Registered confidence`.
    - `docs/scheduled-job-sources.md` contains `does not claim schedule evidence`.
    - `docs/scheduled-job-sources.md` contains `shortcuts run`.
    - `docs/scheduled-job-sources.md` contains `Automator Workflows`.
    - `docs/scheduled-job-sources.md` contains `~/Library/Services`.
    - `docs/scheduled-job-sources.md` contains `~/Library/Workflows`.
    - `docs/scheduled-job-sources.md` contains `does not execute workflows`.
    - `README.md` contains `registered Shortcuts`.
    - `README.md` contains `Automator workflows`.
    - `README.md` contains `registered inventory, not proof of scheduling`.
    - `./script/ci.sh` exits 0.
  </acceptance_criteria>
  <done>Registered-source docs are honest about evidence limits and the full local CI gate passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `/usr/bin/shortcuts list --show-identifiers` -> scanner parser | Command output is parsed as untrusted text and never executed. |
| Workflow bundle metadata -> scanner model | Readable Info.plist metadata is used only for display names and details. |
| Registered records -> user understanding | Listed Shortcuts and workflow files are registered inventory, not scheduling proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Tampering | Shortcuts scanner | avoid | Production runner invokes only `shortcuts list --show-identifiers`; grep verifies no run/view/sign subcommands. |
| T-06-03-02 | Elevation of Privilege | Automator scanner | avoid | Scanner reads bounded user workflow locations and never executes workflows or opens apps. |
| T-06-03-03 | Spoofing | Registered confidence | mitigate | Use `.registered` and explicit "no schedule evidence" schedule text for Shortcuts and Automator records. |
| T-06-03-04 | Information Disclosure | Fixtures and docs | mitigate | Use synthetic names such as `Morning Routine` and `Resize Images`; do not commit live shortcut or workflow details. |
| T-06-03-05 | Denial of Service | Workflow enumeration | mitigate | Enumerate only injected bounded directories and direct children, not arbitrary recursive home scans. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `test -f Sources/ActiveJobsCore/Services/ShortcutsScanner.swift`
- [ ] `test -f Sources/ActiveJobsCore/Services/AutomatorScanner.swift`
- [ ] `rg -n "case shortcuts|case automator" Sources/ActiveJobsCore/Models/ScheduledJob.swift`
- [ ] `rg -n "/usr/bin/shortcuts|\\[\"list\", \"--show-identifiers\"\\]|source: \\.shortcuts|confidence: \\.registered" Sources/ActiveJobsCore/Services/ShortcutsScanner.swift`
- [ ] `! rg -n "shortcuts.*run|shortcuts.*view|shortcuts.*sign" Sources/ActiveJobsCore/Services/ShortcutsScanner.swift`
- [ ] `rg -n "Library/Services|Library/Workflows|source: \\.automator|Automator workflow \\(no schedule evidence\\)" Sources/ActiveJobsCore/Services/AutomatorScanner.swift`
- [ ] `rg -n "ShortcutsScanner\\(\\)|AutomatorScanner\\(homeDirectory: homeDirectory\\)" Sources/ActiveJobsCore/Services/JobScanning.swift`
- [ ] `rg -n "testShortcutsScannerListsRegisteredShortcutsWithoutSchedulingEvidence|testAutomatorScannerListsWorkflowFixturesAsRegistered" Sources/ActiveJobsCoreSelfTest/main.swift`
- [ ] `rg -n "Shortcuts|Automator Workflows|Registered confidence|does not claim schedule evidence" docs/scheduled-job-sources.md`
- [ ] `rg -n "registered Shortcuts|Automator workflows|registered inventory, not proof of scheduling" README.md`
- [ ] `./script/ci.sh`
</verification>

<success_criteria>
- DISC-04 is covered because registered Shortcuts are listed through an injected `shortcuts list --show-identifiers` runner and labeled without schedule evidence.
- DISC-05 is covered because readable Automator workflow files are inventoried from bounded directories and labeled without schedule evidence.
- DISC-07 is covered because missing commands, command failures, missing directories, and unreadable workflow locations become `ScanNote` values.
- QUAL-01 is preserved because scanners do not run, view, sign, edit, execute, or mutate Shortcuts or Automator workflows.
- QUAL-02 is covered because scanner behavior is tested with injected command runners and temporary fixture directories.
- Existing launchd, Hermes cron, cron, search, selection, refresh, and CI behavior continue to pass `./script/ci.sh`.
</success_criteria>

<output>
After completion, create `.planning/phases/06-inventory-model-and-deterministic-sources/06-03-SUMMARY.md`.
</output>
