---
phase: 02-keyboard-navigation
plan: 02-02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [Sources/AutomationHealth/Views/SidebarView.swift]
autonomous: true
requirements: [NAV-01, NAV-02, NAV-03, NAV-04, NAV-06, SRCH-02, SRCH-04, QUAL-01, QUAL-02]
user_setup: []
must_haves:
  truths:
    - "D-01: Down enters at the first visible job when no visible row is selected."
    - "D-02: Up and Down clamp at first and last visible job boundaries."
    - "D-03: Hidden selectedJobID values are treated like no visible selection."
    - "D-04: The sidebar handles Up/Down only when the row/list area is focused."
    - "D-05: The sidebar search field keeps normal text-editing arrow behavior."
    - "D-06: Clicking a sidebar row selects the row and makes the row/list area ready for immediate Up/Down navigation."
    - "D-07: Phase 2 does not add a custom focus ring or new focus visual treatment."
    - "D-08: Search changes do not automatically move selection or replace the detail pane."
    - "D-09: Search-visible selected rows remain the navigation anchor."
    - "D-10: Refresh preservation remains owned by JobStore.refresh()."
    - "D-11: Refresh removal fallback remains JobStore.refresh() selecting the first available refreshed job."
  artifacts:
    - path: "Sources/AutomationHealth/Views/SidebarView.swift"
      provides: "Focused Up/Down key handling over visible sidebar jobs"
      contains: "onKeyPress(.downArrow"
    - path: "Sources/AutomationHealth/Views/SidebarView.swift"
      provides: "Focused Up/Down key handling over visible sidebar jobs"
      contains: "onKeyPress(.upArrow"
  key_links:
    - from: "Sources/AutomationHealth/Views/SidebarView.swift"
      to: "Sources/AutomationHealth/Models/JobPresentation.swift"
      via: "SidebarNavigation.targetJobID"
      pattern: "SidebarNavigation\\.targetJobID"
---

<objective>
Wire focused Up and Down key handling into the existing sidebar selection binding.

Purpose: Let keyboard users move through visible job rows while preserving native search editing and the existing detail-selection data flow.
Output: `SidebarView` handles Up/Down from a focused row/list area, updates `selectedJobID`, and leaves search and refresh behavior in their current owners.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-sidebar-grouping-foundation/01-CONTEXT.md
@.planning/phases/01-sidebar-grouping-foundation/01-02-SUMMARY.md
@.planning/phases/02-keyboard-navigation/02-CONTEXT.md
@.planning/phases/02-keyboard-navigation/02-RESEARCH.md
@.planning/phases/02-keyboard-navigation/02-UI-SPEC.md
@.planning/phases/02-keyboard-navigation/02-PATTERNS.md
@.planning/phases/02-keyboard-navigation/02-01-SUMMARY.md
@Sources/AutomationHealth/Views/SidebarView.swift
@Sources/AutomationHealth/Models/JobPresentation.swift
@Sources/AutomationHealth/Views/ContentView.swift
@Sources/AutomationHealth/Stores/JobStore.swift
</context>

<threat_model>
Trust boundaries: This plan changes SwiftUI interaction code that updates only in-memory UI selection.

STRIDE register:
- threat_id: P2-02-T1
  category: Tampering
  component: `SidebarView` key handling
  disposition: mitigated
  mitigation_plan: Limit key handling effects to assigning the existing `selectedJobID` binding. Do not add scheduler mutation controls, file writes, launchctl calls, Hermes cron writes, or output-file writes.
- threat_id: P2-02-T2
  category: Spoofing
  component: focus ownership
  disposition: mitigated
  mitigation_plan: Handle Up/Down only from the focused row/list area so search-field focus is not spoofed as list focus.
- threat_id: P2-02-T3
  category: Denial of Service
  component: key repeat
  disposition: accepted-low
  mitigation_plan: Each keypress performs one bounded helper lookup over visible rows. No timers, watchers, or background loops are introduced.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Make the sidebar row/list area focusable</name>
  <files>Sources/AutomationHealth/Views/SidebarView.swift</files>
  <read_first>Sources/AutomationHealth/Views/SidebarView.swift, Sources/AutomationHealth/Models/JobPresentation.swift, .planning/phases/02-keyboard-navigation/02-CONTEXT.md, .planning/phases/02-keyboard-navigation/02-UI-SPEC.md</read_first>
  <action>In `SidebarView`, add a private focus enum such as `private enum SidebarFocusTarget: Hashable { case jobList }` and `@FocusState private var focusedTarget: SidebarFocusTarget?`. Apply `.focusable()` and `.focused($focusedTarget, equals: .jobList)` to the scroll/list container that owns the `LazyVStack`, not to `NavigationSplitView` and not to `ContentView`. Add `.accessibilityLabel("Automation list")` if a label is needed for the focusable surface. Do not add custom focus-ring drawing, new visible instructions, or `.searchable` changes.</action>
  <verify>swift build</verify>
  <acceptance_criteria>
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `@FocusState private var focusedTarget`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `SidebarFocusTarget`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `case jobList`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `.focusable()`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `.focused($focusedTarget, equals: .jobList)`.
    - `Sources/AutomationHealth/Views/ContentView.swift` still contains `.searchable(text: $searchText, placement: .sidebar)`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` does not contain `focusEffectDisabled`.
    - `swift build` exits 0.
  </acceptance_criteria>
  <done>The visible job-list surface can receive keyboard focus without changing search ownership or adding custom focus styling.</done>
</task>

<task type="auto">
  <name>Task 2: Route row clicks through selection plus list focus</name>
  <files>Sources/AutomationHealth/Views/SidebarView.swift</files>
  <read_first>Sources/AutomationHealth/Views/SidebarView.swift, .planning/phases/02-keyboard-navigation/02-CONTEXT.md, .planning/phases/02-keyboard-navigation/02-UI-SPEC.md</read_first>
  <action>Change the existing `SidebarJobRow` selection closure from only `selectedJobID = job.id` to a private method such as `private func select(_ job: SidebarJobSummary)`. That method must perform exactly two state updates in order: `selectedJobID = job.id` and `focusedTarget = .jobList`. Keep the write inside the job row action path; do not add click handlers to `SidebarHeader`, `SourceSectionHeader`, the scan footer, or empty-state views. Keep `SidebarJobRow` as a plain button and preserve its selected background logic.</action>
  <verify>swift build</verify>
  <acceptance_criteria>
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `selectedJobID = job.id`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `focusedTarget = .jobList`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `private func select`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` still contains `.buttonStyle(.plain)`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains no `Button` inside `SourceSectionHeader`.
    - `swift build` exits 0.
  </acceptance_criteria>
  <done>Clicking a job row selects it and prepares the list for immediate Up/Down navigation.</done>
</task>

<task type="auto">
  <name>Task 3: Handle Up and Down through the navigation helper</name>
  <files>Sources/AutomationHealth/Views/SidebarView.swift</files>
  <read_first>Sources/AutomationHealth/Views/SidebarView.swift, Sources/AutomationHealth/Models/JobPresentation.swift, .planning/phases/02-keyboard-navigation/02-CONTEXT.md, .planning/phases/02-keyboard-navigation/02-RESEARCH.md</read_first>
  <action>Add `.onKeyPress(.downArrow) { navigate(.next) }` and `.onKeyPress(.upArrow) { navigate(.previous) }` to the focused row/list container. Add `private func navigate(_ direction: SidebarNavigationDirection) -> KeyPress.Result` in `SidebarView`. It must guard `focusedTarget == .jobList`; if not focused, return `.ignored`. It must call `SidebarNavigation.targetJobID(in: visibleJobs, selectedJobID: selectedJobID, direction: direction)`. If the helper returns nil, return `.ignored` without mutating selection. If the returned id equals the current `selectedJobID`, keep `selectedJobID` unchanged and return `.handled` so boundary presses do not move focus away. If the returned id differs, assign `selectedJobID = targetID` and return `.handled`. Do not handle Left, Right, Return, Escape, refresh shortcuts, type-ahead, or command keys.</action>
  <verify>swift build</verify>
  <acceptance_criteria>
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `.onKeyPress(.downArrow)`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `.onKeyPress(.upArrow)`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `private func navigate(_ direction: SidebarNavigationDirection) -> KeyPress.Result`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `guard focusedTarget == .jobList else`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `SidebarNavigation.targetJobID(in: visibleJobs, selectedJobID: selectedJobID, direction: direction)`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `return .handled`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `return .ignored`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` does not contain `.onKeyPress(.leftArrow)`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` does not contain `.onKeyPress(.rightArrow)`.
    - `swift build` exits 0.
  </acceptance_criteria>
  <done>Focused sidebar Up/Down key handling updates the existing selection binding over visible rows only.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `swift build` exits 0.
- [ ] `ContentView` still owns `.searchable(text: $searchText, placement: .sidebar)`.
- [ ] `SidebarView` handles Up and Down only from the focused job-list surface.
- [ ] `SidebarView` uses `SidebarNavigation.targetJobID` with `visibleJobs`.
- [ ] `SidebarView` does not add scheduler IO, job mutation controls, extra shortcuts, or custom focus styling.
</verification>

<success_criteria>
- User can press Down while the sidebar row/list area is focused to select the next visible job.
- User can press Up while the sidebar row/list area is focused to select the previous visible job.
- Summary text, source headers, empty states, and scan footer controls are skipped because navigation uses `visibleJobs`.
- Detail view updates through the existing `selectedJobID` binding.
- Search-field arrow behavior remains native because key handling is scoped to the focused list surface.
</success_criteria>

<output>
After completion, create `.planning/phases/02-keyboard-navigation/02-02-SUMMARY.md`.
</output>
