---
phase: 02-keyboard-navigation
plan: 02-03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified: [Sources/AutomationHealth/Views/SidebarView.swift]
autonomous: true
requirements: [NAV-01, NAV-02, NAV-03, NAV-04, NAV-05, NAV-06, SRCH-02, SRCH-04, QUAL-01, QUAL-02]
user_setup: []
must_haves:
  truths:
    - "D-01: Keyboard navigation can enter the visible list when no visible row is selected."
    - "D-02: Boundary keypresses preserve selection instead of clearing or wrapping."
    - "D-03: Hidden selections under search enter at the first or last visible row on explicit navigation."
    - "D-04: Scroll reveal is tied to list-focused keyboard navigation."
    - "D-05: Search-field arrows are not hijacked by scroll reveal behavior."
    - "D-08: Search changes preserve hidden selection and detail pane until explicit keyboard navigation or row choice."
    - "D-09: Search-visible selected rows remain the navigation anchor."
    - "D-10: After refresh, selected jobs that still exist stay selected and remain usable as navigation anchors."
    - "D-11: If refresh removes the selected job, the existing JobStore.refresh() first-job fallback remains in effect."
    - "D-12: Keyboard navigation scrolls only enough to reveal a newly selected row when it is outside the viewport."
    - "D-13: Already visible keyboard-selected rows do not force extra scroll adjustment."
    - "D-14: Scroll-to-selection behavior applies to keyboard navigation only, not mouse clicks or refresh selection changes."
  artifacts:
    - path: "Sources/AutomationHealth/Views/SidebarView.swift"
      provides: "Keyboard-originated scroll reveal"
      contains: "ScrollViewReader"
    - path: "Sources/AutomationHealth/Stores/JobStore.swift"
      provides: "Existing refresh selection fallback remains unchanged"
      contains: "selectedJobID = refreshed.first?.id"
  key_links:
    - from: "Sources/AutomationHealth/Views/SidebarView.swift"
      to: "Sources/AutomationHealth/Stores/JobStore.swift"
      via: "selectedJobID binding continues to drive JobStore.selectedJob"
      pattern: "selectedJobID"
---

<objective>
Add keyboard-only scroll reveal and final verification for search and refresh interactions.

Purpose: Keep keyboard-selected rows visible without adding jumpy scroll behavior for mouse clicks, search changes, or refresh preservation.
Output: `SidebarView` uses `ScrollViewReader` and keyboard-originated pending target state, with validation evidence recorded in the phase summary.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-sidebar-grouping-foundation/01-CONTEXT.md
@.planning/phases/01-sidebar-grouping-foundation/01-03-SUMMARY.md
@.planning/phases/02-keyboard-navigation/02-CONTEXT.md
@.planning/phases/02-keyboard-navigation/02-RESEARCH.md
@.planning/phases/02-keyboard-navigation/02-UI-SPEC.md
@.planning/phases/02-keyboard-navigation/02-PATTERNS.md
@.planning/phases/02-keyboard-navigation/02-01-SUMMARY.md
@.planning/phases/02-keyboard-navigation/02-02-SUMMARY.md
@Sources/AutomationHealth/Views/SidebarView.swift
@Sources/AutomationHealth/Views/ContentView.swift
@Sources/AutomationHealth/Stores/JobStore.swift
@script/ci.sh
@script/test.sh
</context>

<threat_model>
Trust boundaries: This plan changes local UI scrolling and validation notes. It must not change scheduler data or scanner behavior.

STRIDE register:
- threat_id: P2-03-T1
  category: Tampering
  component: keyboard-originated scroll reveal
  disposition: mitigated
  mitigation_plan: Scroll reveal may update only SwiftUI scroll position and `selectedJobID` through the existing binding. Do not add scheduler writes, output writes, launchctl calls, or Hermes cron mutations.
- threat_id: P2-03-T2
  category: Information Disclosure
  component: sidebar row ids
  disposition: mitigated
  mitigation_plan: Use existing visible row ids only for `ScrollViewReader.scrollTo`; do not expose command text, definitions, output snippets, or file paths in new UI text.
- threat_id: P2-03-T3
  category: Denial of Service
  component: scroll and verification commands
  disposition: accepted-low
  mitigation_plan: Scroll once per handled keyboard movement and run only existing bounded validation commands (`swift build`, `./script/ci.sh`). Do not add watchers or repeated background jobs.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add keyboard-originated pending scroll state</name>
  <files>Sources/AutomationHealth/Views/SidebarView.swift</files>
  <read_first>Sources/AutomationHealth/Views/SidebarView.swift, .planning/phases/02-keyboard-navigation/02-CONTEXT.md, .planning/phases/02-keyboard-navigation/02-UI-SPEC.md, .planning/phases/02-keyboard-navigation/02-PATTERNS.md</read_first>
  <action>Add `@State private var keyboardNavigationTargetID: String?` to `SidebarView`. In `navigate(_:)`, when `SidebarNavigation.targetJobID` returns a non-nil target from a focused keypress, set `keyboardNavigationTargetID = targetID` before returning `.handled`. This must happen for both changed selection and boundary presses; boundary presses may set the current id as the pending target but must not clear selection. Do not set `keyboardNavigationTargetID` in row-click selection, `.onChange(of: sections)`, refresh handling, or search handling.</action>
  <verify>swift build</verify>
  <acceptance_criteria>
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `@State private var keyboardNavigationTargetID: String?`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `keyboardNavigationTargetID = targetID`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `private func navigate(_ direction: SidebarNavigationDirection) -> KeyPress.Result`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` does not contain `.onChange(of: sections)`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` does not contain `.onChange(of: selectedJobID)`.
    - `swift build` exits 0.
  </acceptance_criteria>
  <done>Only keyboard navigation marks a row id for scroll reveal.</done>
</task>

<task type="auto">
  <name>Task 2: Reveal keyboard-selected rows with ScrollViewReader</name>
  <files>Sources/AutomationHealth/Views/SidebarView.swift</files>
  <read_first>Sources/AutomationHealth/Views/SidebarView.swift, .planning/phases/02-keyboard-navigation/02-RESEARCH.md, .planning/phases/02-keyboard-navigation/02-UI-SPEC.md</read_first>
  <action>Wrap the existing `ScrollView` content in `ScrollViewReader { proxy in ... }`. Add `.id(job.id)` to each `SidebarJobRow` so `ScrollViewReader` can target the row. Add `.onChange(of: keyboardNavigationTargetID) { _, targetID in guard let targetID else { return }; proxy.scrollTo(targetID, anchor: nil); keyboardNavigationTargetID = nil }` inside the reader scope. Use `anchor: nil`, not `.center`, so SwiftUI reveals only as needed. Do not wrap source headers in ids used for navigation and do not scroll from mouse row selection.</action>
  <verify>swift build</verify>
  <acceptance_criteria>
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `ScrollViewReader`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `.id(job.id)`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `.onChange(of: keyboardNavigationTargetID)`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `proxy.scrollTo(targetID, anchor: nil)`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `keyboardNavigationTargetID = nil`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` does not contain `anchor: .center`.
    - `swift build` exits 0.
  </acceptance_criteria>
  <done>Keyboard-originated target ids are revealed by SwiftUI without recentering every movement.</done>
</task>

<task type="auto">
  <name>Task 3: Verify search, refresh, and read-only boundaries</name>
  <files>Sources/AutomationHealth/Views/SidebarView.swift, Sources/AutomationHealth/Views/ContentView.swift, Sources/AutomationHealth/Stores/JobStore.swift</files>
  <read_first>Sources/AutomationHealth/Views/SidebarView.swift, Sources/AutomationHealth/Views/ContentView.swift, Sources/AutomationHealth/Stores/JobStore.swift, .planning/phases/02-keyboard-navigation/02-CONTEXT.md, script/ci.sh, script/test.sh</read_first>
  <action>Run `swift build` from the repository root. Then run `./script/ci.sh`. If `./script/ci.sh` still fails in the known pre-existing `testHumanizesSchedulesAndRunTimes()` path, record the exact failing command and fatal-error line in `02-03-SUMMARY.md` and do not weaken Phase 2 behavior to make CI appear green. Inspect the touched files and confirm: `ContentView` still owns `.searchable(text: $searchText, placement: .sidebar)`, `SidebarView` contains no `FileManager`, `Data(contentsOf:)`, `Process`, `/bin/launchctl`, or job mutation controls, and `JobStore.refresh()` still contains `selectedJobID = refreshed.first?.id`. Add manual verification notes in `02-03-SUMMARY.md` for these scenarios: Down selects next visible job, Up selects previous visible job, hidden search selection enters first/last on navigation, boundary presses keep selection, search-field arrows remain normal, keyboard-selected offscreen rows reveal, mouse clicks do not trigger extra scroll reveal, refresh-selected existing job remains selected.</action>
  <verify>swift build && ./script/ci.sh</verify>
  <acceptance_criteria>
    - `swift build` exits 0.
    - `./script/ci.sh` exits 0, or `02-03-SUMMARY.md` records the known unrelated `testHumanizesSchedulesAndRunTimes()` failure with exact failing command and fatal-error line.
    - `Sources/AutomationHealth/Views/ContentView.swift` still contains `.searchable(text: $searchText, placement: .sidebar)`.
    - `Sources/AutomationHealth/Stores/JobStore.swift` still contains `selectedJobID = refreshed.first?.id`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains no `FileManager`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains no `Data(contentsOf:)`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains no `Process`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains no `/bin/launchctl`.
    - `02-03-SUMMARY.md` contains `Down selects next visible job`.
    - `02-03-SUMMARY.md` contains `Search-field arrows remain normal`.
  </acceptance_criteria>
  <done>Phase 2 keyboard navigation has compile validation, CI evidence or documented unrelated CI blocker, and manual behavior evidence.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `swift build` exits 0.
- [ ] `./script/ci.sh` exits 0, or the known unrelated self-test failure is recorded precisely in `02-03-SUMMARY.md`.
- [ ] `SidebarView` uses `ScrollViewReader`, `.id(job.id)`, and `proxy.scrollTo(targetID, anchor: nil)`.
- [ ] Scroll reveal is gated by `keyboardNavigationTargetID`, not by every `selectedJobID` change.
- [ ] `ContentView` still owns search and `JobStore.refresh()` still owns refresh fallback.
- [ ] No scanner IO or scheduled-job mutation behavior is added to SwiftUI views.
</verification>

<success_criteria>
- Keyboard-selected rows are revealed when outside the current viewport.
- Rows that are already visible do not get force-centered on every key movement.
- Search changes and refresh preservation do not create extra scroll movement.
- Detail selection remains synchronized through the existing binding and store lookup.
- The read-only product boundary is preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/02-keyboard-navigation/02-03-SUMMARY.md`.
</output>
