---
phase: 02-keyboard-navigation
plan: 02-01
type: execute
wave: 1
depends_on: []
files_modified: [Sources/AutomationHealth/Models/JobPresentation.swift]
autonomous: true
requirements: [NAV-01, NAV-02, NAV-03, NAV-04, NAV-06, SRCH-02, SRCH-04, QUAL-02]
user_setup: []
must_haves:
  truths:
    - "D-01: When no visible row is selected, Down selects the first visible job and Up selects the last visible job."
    - "D-02: Boundary presses keep the current selection; Up on the first row and Down on the last row do not wrap, clear selection, or move focus away."
    - "D-03: If selectedJobID points to a job hidden by the current search filter, keyboard navigation treats it like no visible selection."
    - "D-04: The helper only computes selection targets; the sidebar view decides when the row/list area is focused."
    - "D-08: Search changes do not force a new selection; hidden selections are preserved until explicit Up/Down navigation or row choice."
    - "D-09: When the selected job remains visible after search, it remains the navigation anchor inside the filtered visible list."
    - "D-14: The helper does not perform scrolling; scroll behavior is keyboard-originated in the view layer."
  artifacts:
    - path: "Sources/AutomationHealth/Models/JobPresentation.swift"
      provides: "Pure visible-row navigation target helper"
      contains: "enum SidebarNavigationDirection"
  key_links:
    - from: "Sources/AutomationHealth/Models/JobPresentation.swift"
      to: "Sources/AutomationHealth/Views/SidebarView.swift"
      via: "SidebarNavigationDirection target selection over visible SidebarJobSummary ids"
      pattern: "SidebarNavigationDirection"
---

<objective>
Define visible-job keyboard navigation behavior for Up and Down movement.

Purpose: Give the sidebar a pure, grep-verifiable helper that chooses the next selected visible job id without touching SwiftUI focus, scrolling, scanner IO, or scheduled job data.
Output: A helper in `Sources/AutomationHealth/Models/JobPresentation.swift` that computes selection targets over the flattened visible sidebar row list.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-sidebar-grouping-foundation/01-CONTEXT.md
@.planning/phases/01-sidebar-grouping-foundation/01-03-SUMMARY.md
@.planning/phases/02-keyboard-navigation/02-CONTEXT.md
@.planning/phases/02-keyboard-navigation/02-RESEARCH.md
@.planning/phases/02-keyboard-navigation/02-UI-SPEC.md
@.planning/phases/02-keyboard-navigation/02-PATTERNS.md
@Sources/AutomationHealth/Models/JobPresentation.swift
</context>

<threat_model>
Trust boundaries: This plan changes only app-target presentation logic over already-visible in-memory row summaries.

STRIDE register:
- threat_id: P2-01-T1
  category: Tampering
  component: visible-row navigation helper
  disposition: mitigated
  mitigation_plan: Do not add filesystem writes, scheduler writes, scanner calls, shell commands, persistence, networking, or job mutation APIs. The helper returns only an optional row id.
- threat_id: P2-01-T2
  category: Information Disclosure
  component: helper inputs
  disposition: mitigated
  mitigation_plan: Accept only `SidebarJobSummary` ids or summaries that are already visible in the sidebar; do not expose command text, definitions, output snippets, or raw scheduler files.
- threat_id: P2-01-T3
  category: Denial of Service
  component: index lookup over visible rows
  disposition: accepted-low
  mitigation_plan: Use a bounded in-memory array traversal over `visibleJobs`. Do not introduce recursive traversal or IO.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add direction and target-selection helper</name>
  <files>Sources/AutomationHealth/Models/JobPresentation.swift</files>
  <read_first>Sources/AutomationHealth/Models/JobPresentation.swift, .planning/phases/02-keyboard-navigation/02-CONTEXT.md, .planning/phases/02-keyboard-navigation/02-UI-SPEC.md, .planning/phases/02-keyboard-navigation/02-PATTERNS.md</read_first>
  <action>Add `enum SidebarNavigationDirection: Sendable { case previous; case next }` in `JobPresentation.swift` near `SidebarJobSummary` and `SidebarJobSection`. Add `enum SidebarNavigation { static func targetJobID(in visibleJobs: [SidebarJobSummary], selectedJobID: String?, direction: SidebarNavigationDirection) -> String? { ... } }`. The implementation must return `nil` when `visibleJobs.isEmpty`. Build `let visibleIDs = visibleJobs.map(\.id)`. If `selectedJobID` is non-nil and `visibleIDs.firstIndex(of: selectedJobID)` succeeds, then `.next` returns `visibleIDs[min(currentIndex + 1, visibleIDs.index(before: visibleIDs.endIndex))]` and `.previous` returns `visibleIDs[max(currentIndex - 1, visibleIDs.startIndex)]`. If `selectedJobID` is nil or hidden from `visibleIDs`, `.next` returns `visibleIDs.first` and `.previous` returns `visibleIDs.last`. Keep the helper internal to the app target; do not import SwiftUI or ActiveJobsCore beyond the existing imports.</action>
  <verify>swift build</verify>
  <acceptance_criteria>
    - `Sources/AutomationHealth/Models/JobPresentation.swift` contains `enum SidebarNavigationDirection: Sendable`.
    - `Sources/AutomationHealth/Models/JobPresentation.swift` contains `case previous`.
    - `Sources/AutomationHealth/Models/JobPresentation.swift` contains `case next`.
    - `Sources/AutomationHealth/Models/JobPresentation.swift` contains `enum SidebarNavigation`.
    - `Sources/AutomationHealth/Models/JobPresentation.swift` contains `static func targetJobID(in visibleJobs: [SidebarJobSummary], selectedJobID: String?, direction: SidebarNavigationDirection) -> String?`.
    - `Sources/AutomationHealth/Models/JobPresentation.swift` contains `guard !visibleJobs.isEmpty else`.
    - `Sources/AutomationHealth/Models/JobPresentation.swift` contains `visibleJobs.map(\\.id)`.
    - `swift build` exits 0.
  </acceptance_criteria>
  <done>`SidebarNavigation.targetJobID` computes previous/next visible row targets without touching UI focus or scanner state.</done>
</task>

<task type="auto">
  <name>Task 2: Encode entry, hidden-selection, and boundary rules in helper logic</name>
  <files>Sources/AutomationHealth/Models/JobPresentation.swift</files>
  <read_first>Sources/AutomationHealth/Models/JobPresentation.swift, .planning/phases/02-keyboard-navigation/02-CONTEXT.md, .planning/phases/02-keyboard-navigation/02-RESEARCH.md</read_first>
  <action>Verify and, if needed, adjust the helper so the concrete Phase 2 rules are represented in code: `SidebarNavigation.targetJobID(in: rows, selectedJobID: nil, direction: .next)` returns the first row id; `.previous` returns the last row id; a selected id that is absent from `visibleJobs.map(\\.id)` follows the same first/last entry behavior; `.previous` from index 0 returns the first id; `.next` from the last index returns the last id. Do not write `nil` for first/last boundaries unless `visibleJobs` is empty. Do not add search-query handling or refresh fallback handling to this helper.</action>
  <verify>swift build</verify>
  <acceptance_criteria>
    - `Sources/AutomationHealth/Models/JobPresentation.swift` contains `visibleIDs.first`.
    - `Sources/AutomationHealth/Models/JobPresentation.swift` contains `visibleIDs.last`.
    - `Sources/AutomationHealth/Models/JobPresentation.swift` contains `firstIndex(of: selectedJobID)`.
    - `Sources/AutomationHealth/Models/JobPresentation.swift` contains `min(`.
    - `Sources/AutomationHealth/Models/JobPresentation.swift` contains `max(`.
    - `Sources/AutomationHealth/Models/JobPresentation.swift` does not contain `FileManager`.
    - `Sources/AutomationHealth/Models/JobPresentation.swift` does not contain `Process`.
    - `swift build` exits 0.
  </acceptance_criteria>
  <done>The helper implements D-01, D-02, and D-03 exactly for visible sidebar rows.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `swift build` exits 0.
- [ ] `SidebarNavigation.targetJobID` returns first/last row ids when the current selected id is nil or hidden.
- [ ] `SidebarNavigation.targetJobID` clamps at first and last visible row boundaries.
- [ ] `SidebarNavigation.targetJobID` uses only visible `SidebarJobSummary` values and does not inspect source headers, search text, store jobs, or scanner IO.
</verification>

<success_criteria>
- Down can be implemented by asking the helper for `.next` over `sections.flatMap(\.jobs)`.
- Up can be implemented by asking the helper for `.previous` over `sections.flatMap(\.jobs)`.
- Hidden selections under search enter the visible list predictably.
- Boundary presses preserve selection instead of clearing or wrapping.
- The helper preserves the read-only product boundary by returning only a UI selection id.
</success_criteria>

<output>
After completion, create `.planning/phases/02-keyboard-navigation/02-01-SUMMARY.md`.
</output>
