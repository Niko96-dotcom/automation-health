---
phase: 09-publication-verification-and-docs-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/privacy-scrub-checklist.md
  - Sources/ActiveJobsCore/Support/JobHumanizer.swift
  - Assets/AppIcon/README.md
  - Assets/AppIcon/automation-health-icon-source.png
  - Assets/AppIcon/AutomationHealth.icns
  - Assets/AppIcon/AutomationHealth.iconset
autonomous: true
requirements: [PRIV-01, VIS-04]
user_setup: []
must_haves:
  truths:
    - "PRIV-01: The publication scrub commands from `docs/privacy-scrub-checklist.md` are run and all unexpected public-surface privacy findings are removed or explicitly documented as accepted checklist guidance false positives."
    - "PRIV-01: Accidental `.DS_Store` files are removed from the repository checkout, including local ignored artifacts under `.git`, `.build`, and `dist`, so the exact scrub command reports no `.DS_Store` paths."
    - "PRIV-01: The personal namespace hit in `Sources/ActiveJobsCore/Support/JobHumanizer.swift` is replaced with private-neutral display-name normalization that does not mention `com.niko`."
    - "VIS-04: README/docs visuals use the committed abstract Pulse Grid app icon assets or explicitly synthetic screenshots only; no public visual exposes real local automations, paths, hostnames, scheduler output, private commands, tokens, or secrets."
    - "VIS-04: `./script/test_app_icon.sh` passes after any docs/assets changes."
  artifacts:
    - path: "docs/privacy-scrub-checklist.md"
      provides: "Rerunnable publication privacy scrub commands and expected false positive policy."
    - path: "Assets/AppIcon/README.md"
      provides: "Pulse Grid public visual identity and source-art constraints."
    - path: "script/test_app_icon.sh"
      provides: "Automated validation for icon assets and public visual safety."
  key_links:
    - from: "publication scrub commands"
      to: "public docs/assets/source surface"
      via: "exact `find` and `rg` commands from `docs/privacy-scrub-checklist.md`"
      pattern: "find . -name .DS_Store -print"
    - from: "Pulse Grid icon source"
      to: "README/docs public visuals"
      via: "abstract sanitized asset reuse instead of real local automation screenshots"
      pattern: "Assets/AppIcon/automation-health-icon-source.png"
---

<objective>
Refresh the public docs/assets surface and make the publication privacy scrub clean enough for final release verification.

Purpose: Close the concrete `PRIV-01` and `VIS-04` blockers before running the final CI/manual publication signoff.

Output: sanitized docs/assets copy, private-neutral display-name normalization, removed accidental `.DS_Store` metadata, passing icon validation, and recorded scrub evidence.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-publication-verification-and-docs-polish/09-RESEARCH.md
@.planning/phases/09-publication-verification-and-docs-polish/09-UI-SPEC.md
@.planning/phases/09-publication-verification-and-docs-polish/09-VALIDATION.md
@.planning/phases/09-publication-verification-and-docs-polish/09-PATTERNS.md
@README.md
@docs/privacy-scrub-checklist.md
@Assets/AppIcon/README.md
@script/test_app_icon.sh
@Sources/ActiveJobsCore/Support/JobHumanizer.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove accidental macOS metadata from the publication surface</name>
  <files>.DS_Store, Sources/.DS_Store, .build/arm64-apple-macosx/debug/.DS_Store, dist/.DS_Store</files>
  <read_first>.gitignore, docs/privacy-scrub-checklist.md, .planning/phases/09-publication-verification-and-docs-polish/09-RESEARCH.md</read_first>
  <action>Run `find . -name .DS_Store -print` from the repository root. Remove every `.DS_Store` path it reports, including `./.DS_Store`, `./Sources/.DS_Store`, `./.build/arm64-apple-macosx/debug/.DS_Store`, `./dist/.DS_Store`, and `./.git/.DS_Store` when present. Treat `.git`, `.build`, and `dist` matches as local ignored artifacts to clean before publication, not source changes to preserve. Confirm `.gitignore` already contains `.DS_Store`; if it does not, add `.DS_Store` as a standalone ignore entry at the top with the other macOS user-state ignores. Do not remove source files, generated icon assets, or implementation changes.</action>
  <verify>
    <automated>test -z "$(find . -name .DS_Store -print)" && git status --short -- .DS_Store Sources/.DS_Store .build dist .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - `find . -name .DS_Store -print` prints no paths.
    - `.gitignore` contains `.DS_Store`.
    - `git status --short -- .DS_Store Sources/.DS_Store` shows no tracked `.DS_Store` additions.
  </acceptance_criteria>
  <done>Publishable source paths no longer contain accidental macOS metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Replace personal namespace display-name cleanup with private-neutral logic</name>
  <files>Sources/ActiveJobsCore/Support/JobHumanizer.swift, Sources/ActiveJobsCoreSelfTest/main.swift</files>
  <read_first>Sources/ActiveJobsCore/Support/JobHumanizer.swift, Sources/ActiveJobsCoreSelfTest/main.swift, docs/privacy-scrub-checklist.md, .planning/phases/09-publication-verification-and-docs-polish/09-PATTERNS.md</read_first>
  <action>Update `JobHumanizer.displayName(_:,source:)` so it no longer contains the literal string `com.niko.`. Preserve the existing public behavior of turning launchd-style identifiers into readable names. A concrete acceptable implementation is to split the raw name on `.` after replacing `-` and `_`, then drop generic bundle-prefix components such as `com`, `org`, `net`, `io`, and `ai` plus placeholder owner components such as `user`, before title-casing the remaining words with the existing acronym and small-word rules. Add or update self-test coverage so a fixture label like `com.example.daily-report` displays as `Example Daily Report` or `Daily Report` according to the chosen generic rule, and so the source no longer needs a personal namespace special case.</action>
  <verify>
    <automated>! rg -n "com\\.niko|/Users/niko" Sources/ActiveJobsCore/Support/JobHumanizer.swift Sources/ActiveJobsCoreSelfTest/main.swift && ./script/test.sh</automated>
  </verify>
  <acceptance_criteria>
    - `Sources/ActiveJobsCore/Support/JobHumanizer.swift` does not contain `com.niko`.
    - `Sources/ActiveJobsCore/Support/JobHumanizer.swift` does not contain `/Users/niko`.
    - `./script/test.sh` exits 0.
  </acceptance_criteria>
  <done>Display-name humanization stays generic and no source code privacy scrub hit remains for the maintainer namespace.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh public docs and visual references around sanitized assets</name>
  <files>README.md, docs/privacy-scrub-checklist.md, Assets/AppIcon/README.md</files>
  <read_first>README.md, docs/privacy-scrub-checklist.md, Assets/AppIcon/README.md, docs/development.md, .planning/phases/09-publication-verification-and-docs-polish/09-UI-SPEC.md, .planning/phases/09-publication-verification-and-docs-polish/09-PATTERNS.md</read_first>
  <action>Review README and docs for public visual language. If adding or changing visuals, use `Assets/AppIcon/automation-health-icon-source.png` or an explicitly synthetic screenshot only. Add concise public copy that says public visuals use abstract app icon assets or synthetic data, and that screenshots must not expose local automation names, paths, hostnames, scheduler output, or private command text. Keep the README practical and source-focused: do not add a marketing hero, decorative card layout, oversized display copy, raw terminal screenshot, or real scheduler screenshot. If `docs/privacy-scrub-checklist.md` needs clarification, add accepted false positive wording that differentiates checklist guidance matches from unexpected private data findings.</action>
  <verify>
    <automated>rg -n "abstract|synthetic|no private data|screenshots|scheduler output|docs/privacy-scrub-checklist.md|Assets/AppIcon|automation-health-icon-source.png" README.md docs/privacy-scrub-checklist.md Assets/AppIcon/README.md && ./script/test_app_icon.sh</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` still contains `docs/privacy-scrub-checklist.md`.
    - `README.md` or `docs/privacy-scrub-checklist.md` contains `synthetic`.
    - `README.md` or `Assets/AppIcon/README.md` contains `automation-health-icon-source.png`.
    - `Assets/AppIcon/README.md` contains `Pulse Grid`.
    - `./script/test_app_icon.sh` exits 0.
  </acceptance_criteria>
  <done>Public docs and visual references point readers toward sanitized abstract or synthetic assets.</done>
</task>

<task type="auto">
  <name>Task 4: Run and record the publication scrub evidence</name>
  <files>.planning/phases/09-publication-verification-and-docs-polish/09-01-SUMMARY.md</files>
  <read_first>docs/privacy-scrub-checklist.md, .planning/phases/09-publication-verification-and-docs-polish/09-VALIDATION.md, .planning/phases/09-publication-verification-and-docs-polish/09-PATTERNS.md</read_first>
  <action>Run the Phase 9 publication scrub commands exactly: `find . -name .DS_Store -print`; `rg -n "niko|com\\.niko|/Users/niko" README.md CONTRIBUTING.md docs .github Sources script`; and `rg -n "token|secret|password|hostname|screenshot|scheduler output" README.md CONTRIBUTING.md docs .github Sources script`. Resolve unexpected findings before continuing. For remaining intentional guidance matches, record each accepted false positive by file and reason in `09-01-SUMMARY.md`; expected categories include the privacy checklist, security/support/contribution guidance, issue templates, and docs that name sensitive categories only as things not to publish. Also run `./script/test_app_icon.sh` and record the pass result.</action>
  <verify>
    <automated>./script/test_app_icon.sh && rg -n "Expected false positives|Privacy scrub|VIS-04|PRIV-01|./script/test_app_icon.sh" .planning/phases/09-publication-verification-and-docs-polish/09-01-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `09-01-SUMMARY.md` contains `PRIV-01`.
    - `09-01-SUMMARY.md` contains `VIS-04`.
    - `09-01-SUMMARY.md` contains `Expected false positives`.
    - `09-01-SUMMARY.md` contains `./script/test_app_icon.sh`.
    - `./script/test_app_icon.sh` exits 0.
  </acceptance_criteria>
  <done>The scrub results and visual safety evidence are recorded for final Phase 9 verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local repository metadata -> public source tree | Accidental Finder metadata must not enter the publishable source surface. |
| Local scheduler identity -> README/docs/assets | Public docs and visuals must not reveal real local automations, paths, hostnames, output, or private commands. |
| Display-name cleanup -> scanner behavior | Removing a personal namespace literal must preserve read-only scanner behavior and avoid adding IO or mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | README/docs/screenshots/sample output | mitigate | Require abstract or synthetic visuals and run scrub commands after docs/assets changes. |
| T-09-02 | Information Disclosure | Source code namespace strings | mitigate | Remove `com.niko` from source and use generic display-name cleanup covered by self-tests. |
| T-09-03 | Information Disclosure | Local repository metadata | mitigate | Remove publishable-tree `.DS_Store` files and confirm `.DS_Store` remains ignored. |
| T-09-04 | Repudiation | Publication scrub evidence | mitigate | Record exact commands, expected false positives, and icon-validation results in `09-01-SUMMARY.md`. |
| T-09-05 | Tampering | Real scheduled jobs | avoid | Plan changes docs/assets/display formatting only and adds no controls that write scheduler files. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `find . -name .DS_Store -print` prints no paths.
- [ ] `! rg -n "com\\.niko|/Users/niko" Sources/ActiveJobsCore/Support/JobHumanizer.swift`
- [ ] `rg -n "synthetic|abstract|no private data|automation-health-icon-source.png" README.md docs/privacy-scrub-checklist.md Assets/AppIcon/README.md`
- [ ] `rg -n "niko|com\\.niko|/Users/niko" README.md CONTRIBUTING.md docs .github Sources script` has no unexpected findings.
- [ ] `rg -n "token|secret|password|hostname|screenshot|scheduler output" README.md CONTRIBUTING.md docs .github Sources script` has only intentional guidance matches or no output.
- [ ] `./script/test_app_icon.sh`
- [ ] `./script/test.sh`
</verification>

<success_criteria>
- PRIV-01 is covered because the publication scrub is run, unexpected findings are fixed, and expected false positives are documented.
- VIS-04 is covered because public visuals rely on abstract Pulse Grid assets or explicitly synthetic screenshots and icon validation passes.
- The app remains read-only and no new scheduler mutation path is introduced.
</success_criteria>
