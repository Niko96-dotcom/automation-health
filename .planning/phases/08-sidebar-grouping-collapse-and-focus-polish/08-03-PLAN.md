---
phase: 08-sidebar-grouping-collapse-and-focus-polish
plan: 03
type: execute
wave: 3
depends_on: ["08-01", "08-02"]
files_modified:
  - Sources/AutomationHealth/Views/SidebarView.swift
  - Sources/AutomationHealth/Views/ContentView.swift
  - Sources/AutomationHealthCore/JobPresentation.swift
  - Sources/ActiveJobsCoreSelfTest/main.swift
autonomous: true
requirements: [SIDE-01, SIDE-02, SIDE-03, SIDE-04, SIDE-05, SIDE-06, SIDE-07]
user_setup:
  - "Manual visual verification is required for focus appearance because the package has no screenshot assertion or UI automation target."
must_haves:
  truths:
    - "D-18: The oversized default blue sidebar focus rectangle is replaced or suppressed while keeping a visible custom keyboard focus cue."
    - "D-19: Selection and focus remain visually distinct: selected rows keep compact accent fill; keyboard focus uses a 1-2 px outline/stroke or equivalent shape cue."
    - "D-20: The focus cue remains visible in dark mode, light mode, active window, inactive window, and high-contrast conditions where practical, using shape or stroke so it does not rely on color alone."
    - "D-21: Prefer SwiftUI focus and overlay styling around `SidebarView`; use minimal AppKit interop only if SwiftUI cannot suppress or replace the default focus rectangle cleanly."
    - "D-22: Plan 08-03 must not redesign the sidebar into cards, oversized headers, decorative backgrounds, or a marketing-style layout."
    - "Final verification must cover all grouping modes, disclosure behavior, search reveal, visible-row keyboard navigation, no-view-IO grep, `./script/ci.sh`, and `./script/build_and_run.sh --verify`."
  artifacts:
    - path: "Sources/AutomationHealth/Views/SidebarView.swift"
      provides: "Custom sidebar focus treatment and final keyboard interaction polish."
    - path: "Sources/AutomationHealth/Views/ContentView.swift"
      provides: "Any final wiring needed for focus/collapse behavior after visual verification."
    - path: "Sources/ActiveJobsCoreSelfTest/main.swift"
      provides: "Final non-UI regression coverage for grouping/navigation behavior."
  key_links:
    - from: "SidebarFocusTarget.jobList"
      to: "custom focus overlay"
      via: "Focus state drives a visible shape/stroke cue while Up/Down remains keyboard-driven."
      pattern: "focusedTarget == .jobList"
    - from: "manual visual checklist"
      to: "Phase 8 validation"
      via: "Focus styling cannot be fully proven by the current self-test executable."
      pattern: "./script/build_and_run.sh --verify"
---

<objective>
Replace the oversized default sidebar focus rectangle with an app-matched accessible focus cue and complete final Phase 8 verification.

Purpose: The richer grouped sidebar should feel native and keyboard-friendly without losing focus visibility or selection clarity.

Output: Custom focus styling, final interaction polish, automated gates, view-boundary grep, app-bundle verification, and a manual checklist for focus/accessibility behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-sidebar-grouping-collapse-and-focus-polish/08-CONTEXT.md
@.planning/phases/08-sidebar-grouping-collapse-and-focus-polish/08-RESEARCH.md
@.planning/phases/08-sidebar-grouping-collapse-and-focus-polish/08-UI-SPEC.md
@.planning/phases/08-sidebar-grouping-collapse-and-focus-polish/08-VALIDATION.md
@.planning/phases/08-sidebar-grouping-collapse-and-focus-polish/08-PATTERNS.md
@Sources/AutomationHealth/Views/SidebarView.swift
@Sources/AutomationHealth/Views/ContentView.swift
@Sources/AutomationHealthCore/JobPresentation.swift
@Sources/ActiveJobsCoreSelfTest/main.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add an accessible custom focus cue around the sidebar list</name>
  <files>Sources/AutomationHealth/Views/SidebarView.swift</files>
  <read_first>Sources/AutomationHealth/Views/SidebarView.swift, .planning/phases/08-sidebar-grouping-collapse-and-focus-polish/08-UI-SPEC.md, .planning/phases/08-sidebar-grouping-collapse-and-focus-polish/08-PATTERNS.md</read_first>
  <action>Keep the sidebar scroll/list area keyboard focusable so `.onKeyPress(.downArrow)` and `.onKeyPress(.upArrow)` still work. Add a custom focus cue driven by `focusedTarget == .jobList`. Preferred implementation: wrap the scroll/list surface or its content container in an overlay with `RoundedRectangle(cornerRadius: 8).strokeBorder(Color.accentColor.opacity(...), lineWidth: 1 or 2)` plus enough shape contrast that focus is not color-only. Use a different opacity for active/inactive window state if the existing environment exposes it cleanly, but do not make inactive focus disappear. Suppress the oversized default blue rectangle using SwiftUI APIs such as `.focusEffectDisabled()` where available on macOS 14. If SwiftUI cannot suppress it cleanly, add the smallest AppKit interop wrapper necessary around the focused list surface. Do not remove `.focusable()`, `.focused($focusedTarget, equals: .jobList)`, or key handlers.</action>
  <verify>
    <automated>rg -n "focusedTarget == \\.jobList|RoundedRectangle|strokeBorder|Color\\.accentColor|lineWidth: 1|lineWidth: 2|focusEffectDisabled|focusable\\(\\)|focused\\(\\$focusedTarget, equals: \\.jobList\\)|onKeyPress\\(\\.downArrow\\)|onKeyPress\\(\\.upArrow\\)" Sources/AutomationHealth/Views/SidebarView.swift && swift build</automated>
  </verify>
  <acceptance_criteria>
    - `SidebarView.swift` contains `focusedTarget == .jobList`.
    - `SidebarView.swift` contains `RoundedRectangle`.
    - `SidebarView.swift` contains `strokeBorder`.
    - `SidebarView.swift` contains `Color.accentColor`.
    - `SidebarView.swift` contains either `lineWidth: 1` or `lineWidth: 2`.
    - `SidebarView.swift` still contains `.focusable()`.
    - `SidebarView.swift` still contains `.focused($focusedTarget, equals: .jobList)`.
    - `SidebarView.swift` still contains `.onKeyPress(.downArrow)` and `.onKeyPress(.upArrow)`.
    - `swift build` exits 0.
  </acceptance_criteria>
  <done>The sidebar keeps keyboard focus and shows a restrained custom focus cue.</done>
</task>

<task type="auto">
  <name>Task 2: Keep row selection, hover, disclosure, and focus visually distinct</name>
  <files>Sources/AutomationHealth/Views/SidebarView.swift</files>
  <read_first>Sources/AutomationHealth/Views/SidebarView.swift, .planning/phases/08-sidebar-grouping-collapse-and-focus-polish/08-UI-SPEC.md</read_first>
  <action>Review and adjust row/header styling so selected rows keep the compact selected fill `Color.accentColor.opacity(0.18)` or an equivalent existing value, hover remains a subtle `Color.primary.opacity(0.06)` or equivalent, disclosure headers remain compact plain buttons, and the focus cue is an outline/stroke rather than a broad filled panel. Ensure section header hover/focus does not resize rows or shift layout. Keep sidebar row minimum height at 38 px. Keep section headers using Label/Body-scale text only; do not introduce display-scale type, large cards, nested cards, decorative backgrounds, gradient orbs, or explanatory paragraphs. Add `.help` and accessibility labels to icon-only grouping/disclosure controls if any icon-only buttons remain.</action>
  <verify>
    <automated>rg -n "Color\\.accentColor\\.opacity\\(0\\.18\\)|Color\\.primary\\.opacity\\(0\\.06\\)|buttonStyle\\(\\.plain\\)|minHeight: 38|help\\(|accessibilityLabel" Sources/AutomationHealth/Views/SidebarView.swift && ! rg -n "font\\(\\.largeTitle|font\\(\\.title|LinearGradient|RadialGradient|\\.background\\(.*card|Display|marketing|Group description" Sources/AutomationHealth/Views/SidebarView.swift</automated>
  </verify>
  <acceptance_criteria>
    - `SidebarView.swift` contains `Color.accentColor.opacity(0.18)`.
    - `SidebarView.swift` contains `Color.primary.opacity(0.06)`.
    - `SidebarView.swift` contains `buttonStyle(.plain)`.
    - `SidebarView.swift` contains `minHeight: 38`.
    - `SidebarView.swift` contains `help(`.
    - `SidebarView.swift` contains `accessibilityLabel`.
    - `SidebarView.swift` does not contain `LinearGradient` or `RadialGradient`.
    - `SidebarView.swift` does not contain `.largeTitle` or `.title` for sidebar additions.
  </acceptance_criteria>
  <done>Selection, hover, disclosure, and focus states remain compact and visually distinct.</done>
</task>

<task type="auto">
  <name>Task 3: Run final automated Phase 8 gates</name>
  <files>Sources/AutomationHealth/Views/SidebarView.swift, Sources/AutomationHealth/Views/ContentView.swift, Sources/AutomationHealthCore/JobPresentation.swift, Sources/ActiveJobsCoreSelfTest/main.swift</files>
  <read_first>Sources/AutomationHealth/Views/SidebarView.swift, Sources/AutomationHealth/Views/ContentView.swift, Sources/AutomationHealthCore/JobPresentation.swift, Sources/ActiveJobsCoreSelfTest/main.swift, .planning/phases/08-sidebar-grouping-collapse-and-focus-polish/08-VALIDATION.md</read_first>
  <action>Run the final automated checks for the phase. First, run `./script/test.sh` and confirm the Phase 8 self-tests pass. Then run the view-layer IO grep exactly: `! rg -n "Data\\(contentsOf:|FileManager\\.default|contentsOfDirectory|JSONEncoder|JSONDecoder|crontab|shortcuts list|launchctl" Sources/AutomationHealth/Views`. Then run `./script/ci.sh`. Finally run `./script/build_and_run.sh --verify` to verify the local app bundle still builds and launches through the existing script. If any command fails, fix the implementation before moving to manual visual verification.</action>
  <verify>
    <automated>./script/test.sh && ! rg -n "Data\\(contentsOf:|FileManager\\.default|contentsOfDirectory|JSONEncoder|JSONDecoder|crontab|shortcuts list|launchctl" Sources/AutomationHealth/Views && ./script/ci.sh && ./script/build_and_run.sh --verify</automated>
  </verify>
  <acceptance_criteria>
    - `./script/test.sh` exits 0.
    - The view-layer IO grep exits 1, proving no matching IO terms were found in `Sources/AutomationHealth/Views`.
    - `./script/ci.sh` exits 0.
    - `./script/build_and_run.sh --verify` exits 0.
  </acceptance_criteria>
  <done>The automated Phase 8 gates and app-bundle verification pass.</done>
</task>

<task type="manual">
  <name>Task 4: Manually verify grouping, collapse, keyboard navigation, and focus styling</name>
  <files>dist/AutomationHealth.app</files>
  <read_first>Sources/AutomationHealth/Views/SidebarView.swift, Sources/AutomationHealth/Views/ContentView.swift, .planning/phases/08-sidebar-grouping-collapse-and-focus-polish/08-UI-SPEC.md, .planning/phases/08-sidebar-grouping-collapse-and-focus-polish/08-VALIDATION.md</read_first>
  <action>Launch the verified app and perform this checklist with synthetic or privacy-safe local inventory only. Switch grouping modes in this exact order: `Source`, `Origin`, `Health`, `Trigger`, `Confidence`; confirm section order, counts, labels, and subtitles match `08-UI-SPEC.md`. In `Origin`, confirm launchd rows split by `User-authored`, `Third-party app`, `System`, or `Unknown` when fixture data is available and row subtitles still show `launchd`. Collapse a section containing the selected row and confirm the detail pane continues to show that selected automation record. Collapse a section, search for a row inside it, confirm the row appears, clear search, and confirm the section returns to collapsed. Focus the sidebar and use Up/Down while sections are collapsed and while search is active; confirm selection moves only to visible rows. Verify the default oversized blue focus rectangle is gone. Verify the custom focus cue remains visible and distinct from selection in dark mode, light mode, inactive window state, and high-contrast mode where practical. Record the checklist result in the execution summary for `08-03`.</action>
  <verify>
    <manual>`./script/build_and_run.sh --verify` exits 0 and the manual checklist above is recorded in `08-03-SUMMARY.md` after execution.</manual>
  </verify>
  <acceptance_criteria>
    - All five grouping modes are manually verified in UI order.
    - Collapse/expand preserves selected detail.
    - Search reveals matching collapsed rows and clearing search restores collapse state.
    - Up/Down navigation skips collapsed and search-hidden rows.
    - The oversized default blue focus rectangle is gone.
    - A visible custom focus cue remains in dark mode, light mode, inactive window state, and high-contrast mode where practical.
  </acceptance_criteria>
  <done>Human-visible Phase 8 behavior matches the approved UI design contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Keyboard focus -> selected job | Focus styling must not hide focus or silently change selected automation records. |
| Visual polish -> app shell | Focus polish should stay scoped to the sidebar and avoid broad layout redesign. |
| Final verification -> real automations | Verification must not mutate scheduler files, scripts, Shortcuts, Automator workflows, cron entries, launchd plists, or Hermes metadata. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | Final UI verification | avoid | Verification uses existing build/run scripts and does not add edit/run/delete scheduler behavior. |
| T-08-02 | Information Disclosure | SwiftUI views | mitigate | Final grep verifies no direct scheduler/file/process IO terms appear in view files. |
| T-08-03 | Spoofing | Visual evidence labels | mitigate | Final manual checks verify source/origin/confidence/trigger grouping labels remain visible and conservative. |
| T-08-04 | Repudiation | Hidden selected detail | mitigate | Manual checks confirm collapsing/searching does not silently select another record. |
| T-08-05 | Denial of Service | Focus accessibility | mitigate | The focus cue is visible by shape/stroke and manual checks cover keyboard navigation plus high-contrast where practical. |
| T-08-06 | Information Disclosure | Persisted preferences | avoid | Plan adds no preference persistence and final grep/checks can confirm no `UserDefaults` or `@AppStorage` were introduced for grouping/collapse. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `rg -n "focusedTarget == \\.jobList|RoundedRectangle|strokeBorder|focusable\\(\\)|onKeyPress\\(\\.downArrow\\)|onKeyPress\\(\\.upArrow\\)" Sources/AutomationHealth/Views/SidebarView.swift`
- [ ] `rg -n "Color\\.accentColor\\.opacity\\(0\\.18\\)|buttonStyle\\(\\.plain\\)|minHeight: 38|accessibilityLabel" Sources/AutomationHealth/Views/SidebarView.swift`
- [ ] `! rg -n "Data\\(contentsOf:|FileManager\\.default|contentsOfDirectory|JSONEncoder|JSONDecoder|crontab|shortcuts list|launchctl" Sources/AutomationHealth/Views`
- [ ] `./script/test.sh`
- [ ] `./script/ci.sh`
- [ ] `./script/build_and_run.sh --verify`
- [ ] Manual checklist from Task 4 recorded in `08-03-SUMMARY.md`
</verification>

<success_criteria>
- SIDE-06 is covered because the oversized default focus rectangle is replaced or suppressed by an app-matched custom focus cue.
- SIDE-07 is covered because keyboard focus remains visible and manual checks cover dark, light, inactive, and high-contrast states where practical.
- SIDE-01 through SIDE-05 remain covered by final manual and automated regression checks across grouping, collapse, search, ordering, origin separation, and visible-row navigation.
- The app remains read-only, compact, native, and free of new third-party dependencies.
</success_criteria>
