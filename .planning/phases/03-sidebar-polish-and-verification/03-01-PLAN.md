---
phase: 03-sidebar-polish-and-verification
plan: 03-01
type: execute
wave: 1
depends_on: []
files_modified: [Sources/AutomationHealth/Views/ContentView.swift, Sources/AutomationHealth/Views/SidebarView.swift]
autonomous: true
requirements: [ORG-04, SRCH-03]
user_setup: []
must_haves:
  truths:
    - "D-01: Source headers use compact leading label plus trailing count with a subtle divider, not parenthetical counts or badges."
    - "D-02: Header styling stays quiet and native-feeling; source order and visible counts remain owned by SidebarJobSection.sections(for:)."
    - "D-03: Job rows keep display name, subtitle, health dot, and selected-row behavior."
    - "D-04: Selected rows keep the soft Color.accentColor fill treatment."
    - "D-05: Hover polish is quiet, local to SidebarJobRow, and does not add custom focus rings."
    - "D-06: A non-empty search query with no visible jobs shows a small inline sidebar empty state."
    - "D-07: The filtered empty state is only for filtered searches, not the broader no-jobs scan state."
    - "D-08: The empty state is not a selectable placeholder row and is not part of visibleJobs."
    - "D-09: Empty-state copy is exactly No matching automations and Try a different search."
  artifacts:
    - path: "Sources/AutomationHealth/Views/ContentView.swift"
      provides: "Filtered-search empty-state signal"
      contains: "showsFilteredEmptyState"
    - path: "Sources/AutomationHealth/Views/SidebarView.swift"
      provides: "Polished headers, rows, and inline filtered empty state"
      contains: "FilteredSidebarEmptyState"
  key_links:
    - from: "Sources/AutomationHealth/Views/ContentView.swift"
      to: "Sources/AutomationHealth/Views/SidebarView.swift"
      via: "showsFilteredEmptyState"
      pattern: "showsFilteredEmptyState"
---

<objective>
Polish grouped sidebar headers and rows, and add the filtered-search empty state.

Purpose: Make the source-grouped sidebar cleaner and more native-feeling while preserving row content, selection, keyboard navigation, and the scanner/read-only boundary.
Output: `ContentView` passes a filtered-empty signal and `SidebarView` renders compact section headers, lightly polished job rows, and a non-selectable inline empty state for no-result searches.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-sidebar-grouping-foundation/01-CONTEXT.md
@.planning/phases/02-keyboard-navigation/02-CONTEXT.md
@.planning/phases/03-sidebar-polish-and-verification/03-CONTEXT.md
@.planning/phases/03-sidebar-polish-and-verification/03-RESEARCH.md
@.planning/phases/03-sidebar-polish-and-verification/03-UI-SPEC.md
@.planning/phases/03-sidebar-polish-and-verification/03-PATTERNS.md
@Sources/AutomationHealth/Views/ContentView.swift
@Sources/AutomationHealth/Views/SidebarView.swift
@Sources/AutomationHealth/Models/JobPresentation.swift
</context>

<threat_model>
Trust boundaries: This plan changes SwiftUI sidebar presentation over already-loaded in-memory job summaries.

STRIDE register:
- threat_id: P3-01-T1
  category: Tampering
  component: sidebar presentation
  disposition: mitigated
  mitigation_plan: Limit changes to `ContentView` and `SidebarView`. Do not add scheduler writes, launchd mutation, Hermes cron metadata writes, output-file writes, persistence, networking, or scanner calls.
- threat_id: P3-01-T2
  category: Information Disclosure
  component: filtered empty state and headers
  disposition: mitigated
  mitigation_plan: Render only section title, visible count, row display name, row subtitle, and existing health dot. Do not expose command text, definitions, output snippets, or filesystem paths in new sidebar copy.
- threat_id: P3-01-T3
  category: Denial of Service
  component: search-empty derivation and hover rendering
  disposition: accepted-low
  mitigation_plan: Derive `showsFilteredEmptyState` from existing in-memory `searchText` and `filteredJobs`; hover state is per-row SwiftUI state only. Do not add timers, file watchers, background tasks, or extra scanner refreshes.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Pass a filtered-search empty-state signal into SidebarView</name>
  <files>Sources/AutomationHealth/Views/ContentView.swift, Sources/AutomationHealth/Views/SidebarView.swift</files>
  <read_first>Sources/AutomationHealth/Views/ContentView.swift, Sources/AutomationHealth/Views/SidebarView.swift, Sources/AutomationHealth/Models/JobPresentation.swift, .planning/phases/03-sidebar-polish-and-verification/03-CONTEXT.md, .planning/phases/03-sidebar-polish-and-verification/03-UI-SPEC.md</read_first>
  <action>In `ContentView`, add `private var hasSearchQuery: Bool { !searchText.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty }` near `filteredJobs`. Add `private var showsFilteredEmptyState: Bool { hasSearchQuery && filteredJobs.isEmpty }`. Update the `SidebarView(...)` call to pass `showsFilteredEmptyState: showsFilteredEmptyState`. In `SidebarView`, add `let showsFilteredEmptyState: Bool` after `let sections: [SidebarJobSection]`. Do not move `.searchable(text: $searchText, placement: .sidebar)`, do not duplicate search filtering inside `SidebarView`, and do not change `SidebarJobSection.sections(for:)`.</action>
  <verify>swift build</verify>
  <acceptance_criteria>
    - `Sources/AutomationHealth/Views/ContentView.swift` contains `private var hasSearchQuery: Bool`.
    - `Sources/AutomationHealth/Views/ContentView.swift` contains `!searchText.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty`.
    - `Sources/AutomationHealth/Views/ContentView.swift` contains `private var showsFilteredEmptyState: Bool`.
    - `Sources/AutomationHealth/Views/ContentView.swift` contains `hasSearchQuery && filteredJobs.isEmpty`.
    - `Sources/AutomationHealth/Views/ContentView.swift` contains `showsFilteredEmptyState: showsFilteredEmptyState`.
    - `Sources/AutomationHealth/Views/ContentView.swift` still contains `.searchable(text: $searchText, placement: .sidebar)`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `let showsFilteredEmptyState: Bool`.
    - `swift build` exits 0.
  </acceptance_criteria>
  <done>`ContentView` owns the filtered-empty decision and `SidebarView` receives it as presentation state.</done>
</task>

<task type="auto">
  <name>Task 2: Render the non-selectable filtered empty state</name>
  <files>Sources/AutomationHealth/Views/SidebarView.swift</files>
  <read_first>Sources/AutomationHealth/Views/SidebarView.swift, Sources/AutomationHealth/Models/JobPresentation.swift, .planning/phases/02-keyboard-navigation/02-CONTEXT.md, .planning/phases/03-sidebar-polish-and-verification/03-CONTEXT.md, .planning/phases/03-sidebar-polish-and-verification/03-UI-SPEC.md, .planning/phases/03-sidebar-polish-and-verification/03-PATTERNS.md</read_first>
  <action>Inside the `LazyVStack`, after `SidebarHeader(...)` and before `ForEach(sections)`, add `if showsFilteredEmptyState { FilteredSidebarEmptyState() }`. Add a private `FilteredSidebarEmptyState: View` in `SidebarView.swift`. Its body must render `Text("No matching automations")` with a compact semibold system font and `Text("Try a different search.")` with secondary body styling. Use a plain `VStack(alignment: .leading, spacing: 8)` with sidebar-scale padding such as `.padding(.horizontal, 8)` and `.padding(.vertical, 24)`. Do not make it a `Button`, do not attach `.id(...)`, do not append it to `visibleJobs`, and do not change `SidebarNavigation.targetJobID`.</action>
  <verify>swift build</verify>
  <acceptance_criteria>
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `if showsFilteredEmptyState`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `private struct FilteredSidebarEmptyState: View`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `Text("No matching automations")`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `Text("Try a different search.")`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `VStack(alignment: .leading, spacing: 8)`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` does not contain `FilteredSidebarEmptyState().id`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` does not contain `Button(action: {})`.
    - `Sources/AutomationHealth/Models/JobPresentation.swift` still contains `guard !visibleJobs.isEmpty else`.
    - `swift build` exits 0.
  </acceptance_criteria>
  <done>Filtered no-result searches show inline copy without adding a selectable row or navigation target.</done>
</task>

<task type="auto">
  <name>Task 3: Replace parenthetical section counts with compact label-plus-count headers</name>
  <files>Sources/AutomationHealth/Views/SidebarView.swift</files>
  <read_first>Sources/AutomationHealth/Views/SidebarView.swift, Sources/AutomationHealth/Models/JobPresentation.swift, .planning/phases/01-sidebar-grouping-foundation/01-CONTEXT.md, .planning/phases/03-sidebar-polish-and-verification/03-CONTEXT.md, .planning/phases/03-sidebar-polish-and-verification/03-UI-SPEC.md, .planning/phases/03-sidebar-polish-and-verification/03-PATTERNS.md</read_first>
  <action>Update `SourceSectionHeader` so it no longer renders `Text("\(section.title) (\(section.visibleCount))")`. Use a `VStack(alignment: .leading, spacing: 4)` containing an `HStack(alignment: .firstTextBaseline, spacing: 8)` with `Text(section.title)`, `Spacer(minLength: 8)`, and `Text("\(section.visibleCount)")`. Style both header texts with `.font(.system(size: 11, weight: .semibold))`, `.foregroundStyle(.secondary)`, and `.lineLimit(1)`. Apply `.monospacedDigit()` to the count. Add a subtle `Divider().opacity(0.35)` below the label/count line. Keep the header non-selectable: no `Button`, no click handler, no `.focused`, and no row id.</action>
  <verify>swift build</verify>
  <acceptance_criteria>
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `HStack(alignment: .firstTextBaseline, spacing: 8)`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `Text(section.title)`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `Text("\\(section.visibleCount)")`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `.monospacedDigit()`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `Divider()`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `.opacity(0.35)`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` does not contain `Text("\\(section.title) (\\(section.visibleCount))")`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains no `Button` inside `SourceSectionHeader`.
    - `swift build` exits 0.
  </acceptance_criteria>
  <done>Source headers use quiet label-plus-count structure with a subtle divider and no selectable behavior.</done>
</task>

<task type="auto">
  <name>Task 4: Add light row hover polish while preserving selected-row content and behavior</name>
  <files>Sources/AutomationHealth/Views/SidebarView.swift</files>
  <read_first>Sources/AutomationHealth/Views/SidebarView.swift, .planning/phases/03-sidebar-polish-and-verification/03-CONTEXT.md, .planning/phases/03-sidebar-polish-and-verification/03-UI-SPEC.md, .planning/phases/03-sidebar-polish-and-verification/03-PATTERNS.md</read_first>
  <action>In `SidebarJobRow`, add `@State private var isHovered = false`. Replace the current `selectionBackground` helper with `private var rowBackground: Color` that returns `Color.accentColor.opacity(0.18)` when `isSelected`, returns `Color.primary.opacity(0.06)` when `isHovered`, and returns `.clear` otherwise. Change `.background(selectionBackground, in: RoundedRectangle(cornerRadius: 6))` to `.background(rowBackground, in: RoundedRectangle(cornerRadius: 6))` and add `.onHover { isHovered = $0 }` to the button or row content. Keep `HealthDot(kind: job.healthKind)`, `Text(job.displayName)`, `Text(job.subtitle)`, `.buttonStyle(.plain)`, `.lineLimit(1)`, and the existing `select` action. Do not add a custom focus ring or new focus visual treatment.</action>
  <verify>swift build</verify>
  <acceptance_criteria>
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `@State private var isHovered = false`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `.onHover`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `private var rowBackground: Color`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `Color.accentColor.opacity(0.18)`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `Color.primary.opacity(0.06)`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `HealthDot(kind: job.healthKind)`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `Text(job.displayName)`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `Text(job.subtitle)`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` still contains `.buttonStyle(.plain)`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` does not contain `focusEffectDisabled`.
    - `swift build` exits 0.
  </acceptance_criteria>
  <done>Rows gain a quiet hover surface while preserving display content, health dot, plain-button behavior, and soft selected fill.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `swift build` exits 0.
- [ ] `ContentView` still owns search through `.searchable(text: $searchText, placement: .sidebar)`.
- [ ] `SidebarView.visibleJobs` still equals `sections.flatMap(\.jobs)`.
- [ ] The filtered empty state uses exactly `No matching automations` and `Try a different search.`
- [ ] `SourceSectionHeader` no longer renders parenthetical counts.
- [ ] `SidebarJobRow` still renders `HealthDot`, `job.displayName`, `job.subtitle`, and the soft selected accent fill.
- [ ] `SidebarView.swift` contains no `FileManager`, `Data(contentsOf:)`, `Process`, `/bin/launchctl`, scheduler mutation APIs, or new job management controls.
</verification>

<success_criteria>
- ORG-04 is satisfied because grouped job rows keep display name, subtitle, health dot, and selected styling.
- SRCH-03 is satisfied because no-result searches show a non-selectable inline empty state.
- Section headers read as compact source labels with visible counts and subtle separation.
- The sidebar remains compact and native-feeling at the current split-view width.
- The read-only scanner boundary remains intact.
</success_criteria>

<output>
After completion, create `.planning/phases/03-sidebar-polish-and-verification/03-01-SUMMARY.md`.
</output>
