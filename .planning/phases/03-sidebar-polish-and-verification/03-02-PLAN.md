---
phase: 03-sidebar-polish-and-verification
plan: 03-02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: []
autonomous: false
requirements: [ORG-04, SRCH-03, QUAL-04]
user_setup: []
must_haves:
  truths:
    - "D-03: Rows still show display name, subtitle, health dot, and selected-row behavior after polish."
    - "D-08: The filtered empty state is not selectable and Up/Down does nothing when there are no visible jobs."
    - "D-10: The automated gate for Phase 3 is ./script/ci.sh."
    - "D-11: Manual verification covers row content, selection styling, search no-results, clearing search, and grouped Up/Down navigation."
    - "D-12: Manual verification is documented in the phase summary only; no new checklist document or script is added."
    - "D-13: If CI fails because of unrelated pre-existing changes or unrelated self-test failures, isolate and report the failure instead of hiding it."
  artifacts:
    - path: "script/ci.sh"
      provides: "Local automated verification gate"
      contains: "swift build"
    - path: ".planning/phases/03-sidebar-polish-and-verification/03-02-SUMMARY.md"
      provides: "CI and manual sidebar verification evidence"
      contains: "Manual sidebar behavior check"
  key_links:
    - from: "Sources/AutomationHealth/Views/SidebarView.swift"
      to: "script/ci.sh"
      via: "Phase 3 UI changes verified by local CI"
      pattern: "./script/ci.sh"
---

<objective>
Run the Phase 3 verification gate and record manual sidebar behavior evidence.

Purpose: Prove the polished sidebar still satisfies row, empty-state, keyboard-navigation, and build/test requirements.
Output: A completed `03-02-SUMMARY.md` with `./script/ci.sh` evidence and manual sidebar behavior notes.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-sidebar-grouping-foundation/01-CONTEXT.md
@.planning/phases/02-keyboard-navigation/02-CONTEXT.md
@.planning/phases/03-sidebar-polish-and-verification/03-CONTEXT.md
@.planning/phases/03-sidebar-polish-and-verification/03-RESEARCH.md
@.planning/phases/03-sidebar-polish-and-verification/03-UI-SPEC.md
@.planning/phases/03-sidebar-polish-and-verification/03-PATTERNS.md
@.planning/phases/03-sidebar-polish-and-verification/03-01-SUMMARY.md
@Sources/AutomationHealth/Views/ContentView.swift
@Sources/AutomationHealth/Views/SidebarView.swift
@Sources/AutomationHealth/Models/JobPresentation.swift
@Sources/AutomationHealth/Stores/JobStore.swift
@script/ci.sh
@script/test.sh
</context>

<threat_model>
Trust boundaries: This plan runs local verification and records evidence. It should not alter scheduler data.

STRIDE register:
- threat_id: P3-02-T1
  category: Tampering
  component: verification workflow
  disposition: mitigated
  mitigation_plan: Run existing read/build/test commands only. Do not edit scheduler files, launchd state, Hermes cron metadata, output files, or scanner inputs during verification.
- threat_id: P3-02-T2
  category: Repudiation
  component: manual check evidence
  disposition: mitigated
  mitigation_plan: Record exact commands, pass/fail outcomes, and manual sidebar scenarios in `03-02-SUMMARY.md` so verification evidence is auditable.
- threat_id: P3-02-T3
  category: Denial of Service
  component: local CI
  disposition: accepted-low
  mitigation_plan: Use the existing bounded `./script/ci.sh` gate once after implementation. Do not add watchers, repeated background builds, or long-running automation.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Run the local CI gate</name>
  <files>script/ci.sh, script/test.sh, Sources/AutomationHealth/Views/ContentView.swift, Sources/AutomationHealth/Views/SidebarView.swift, Sources/AutomationHealth/Models/JobPresentation.swift</files>
  <read_first>script/ci.sh, script/test.sh, Sources/AutomationHealth/Views/ContentView.swift, Sources/AutomationHealth/Views/SidebarView.swift, Sources/AutomationHealth/Models/JobPresentation.swift, .planning/phases/03-sidebar-polish-and-verification/03-CONTEXT.md</read_first>
  <action>Run `./script/ci.sh` from the repository root. If it exits 0, record the command and pass result in `03-02-SUMMARY.md`. If it exits non-zero, inspect the output and determine whether the failure is caused by Phase 3 changes. Fix any Phase 3-caused compile or self-test failure before completing this task. If the failure is unrelated or pre-existing, record the exact command, failing test or compiler diagnostic, and why it is unrelated; also record whether it blocks Phase 3 acceptance under QUAL-04. Do not edit `script/ci.sh` or `script/test.sh` unless Phase 3 directly broke them.</action>
  <verify>./script/ci.sh</verify>
  <acceptance_criteria>
    - `./script/ci.sh` exits 0, or `.planning/phases/03-sidebar-polish-and-verification/03-02-SUMMARY.md` contains `CI blocked by unrelated failure`.
    - If CI exits non-zero, `.planning/phases/03-sidebar-polish-and-verification/03-02-SUMMARY.md` contains the exact failing command.
    - If CI exits non-zero, `.planning/phases/03-sidebar-polish-and-verification/03-02-SUMMARY.md` contains the failing diagnostic or test name.
    - `script/ci.sh` still contains `swift build`.
    - `script/ci.sh` still contains `./script/test.sh`.
  </acceptance_criteria>
  <done>The local CI gate has pass evidence or an isolated unrelated blocker recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Verify source-level sidebar contracts</name>
  <files>Sources/AutomationHealth/Views/ContentView.swift, Sources/AutomationHealth/Views/SidebarView.swift, Sources/AutomationHealth/Models/JobPresentation.swift, Sources/AutomationHealth/Stores/JobStore.swift</files>
  <read_first>Sources/AutomationHealth/Views/ContentView.swift, Sources/AutomationHealth/Views/SidebarView.swift, Sources/AutomationHealth/Models/JobPresentation.swift, Sources/AutomationHealth/Stores/JobStore.swift, .planning/phases/03-sidebar-polish-and-verification/03-UI-SPEC.md</read_first>
  <action>Inspect source after Plan 03-01. Confirm `SidebarView` still contains `HealthDot(kind: job.healthKind)`, `Text(job.displayName)`, `Text(job.subtitle)`, `Color.accentColor.opacity(0.18)`, `.buttonStyle(.plain)`, `visibleJobs`, and `sections.flatMap(\\.jobs)`. Confirm `ContentView` still contains `.searchable(text: $searchText, placement: .sidebar)` and `showsFilteredEmptyState`. Confirm `SidebarView.swift` contains `No matching automations` and `Try a different search.` Confirm `SidebarView.swift` contains no `FileManager`, `Data(contentsOf:)`, `Process`, `/bin/launchctl`, `launchctl`, `StandardOutPath`, `StandardErrorPath`, or job mutation controls. Record these checks in `03-02-SUMMARY.md`.</action>
  <verify>grep -n "No matching automations\\|Try a different search\\.\\|HealthDot(kind: job.healthKind)\\|Text(job.displayName)\\|Text(job.subtitle)" Sources/AutomationHealth/Views/SidebarView.swift</verify>
  <acceptance_criteria>
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `HealthDot(kind: job.healthKind)`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `Text(job.displayName)`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `Text(job.subtitle)`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `No matching automations`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `Try a different search.`
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains `sections.flatMap(\\.jobs)`.
    - `Sources/AutomationHealth/Views/ContentView.swift` contains `.searchable(text: $searchText, placement: .sidebar)`.
    - `Sources/AutomationHealth/Views/ContentView.swift` contains `showsFilteredEmptyState`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains no `FileManager`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains no `Data(contentsOf:)`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains no `Process`.
    - `Sources/AutomationHealth/Views/SidebarView.swift` contains no `/bin/launchctl`.
  </acceptance_criteria>
  <done>The final source state still satisfies row, empty-state, navigation, search, and read-only boundaries.</done>
</task>

<task type="manual">
  <name>Task 3: Perform and document the manual sidebar behavior check</name>
  <files>.planning/phases/03-sidebar-polish-and-verification/03-02-SUMMARY.md, Sources/AutomationHealth/Views/ContentView.swift, Sources/AutomationHealth/Views/SidebarView.swift</files>
  <read_first>.planning/phases/03-sidebar-polish-and-verification/03-CONTEXT.md, .planning/phases/03-sidebar-polish-and-verification/03-UI-SPEC.md, Sources/AutomationHealth/Views/ContentView.swift, Sources/AutomationHealth/Views/SidebarView.swift</read_first>
  <action>Launch or inspect the app using the existing local workflow available in this repository. Perform this manual sidebar behavior check and record the result in `03-02-SUMMARY.md` under a heading exactly named `Manual sidebar behavior check`: (1) grouped rows still show display name, subtitle, health dot, and soft selected-row styling; (2) a search query with no visible jobs shows `No matching automations` and `Try a different search.` inside the sidebar list area; (3) the empty state is not selectable and Up/Down does not select a placeholder when no visible jobs exist; (4) clearing search restores grouped rows; (5) Up/Down navigation over the restored grouped rows still works and skips headers and footer. Do not create a separate checklist file or a new script.</action>
  <verify>test -f .planning/phases/03-sidebar-polish-and-verification/03-02-SUMMARY.md && grep -n "Manual sidebar behavior check" .planning/phases/03-sidebar-polish-and-verification/03-02-SUMMARY.md</verify>
  <acceptance_criteria>
    - `.planning/phases/03-sidebar-polish-and-verification/03-02-SUMMARY.md` contains `Manual sidebar behavior check`.
    - `.planning/phases/03-sidebar-polish-and-verification/03-02-SUMMARY.md` contains `display name, subtitle, health dot`.
    - `.planning/phases/03-sidebar-polish-and-verification/03-02-SUMMARY.md` contains `soft selected-row styling`.
    - `.planning/phases/03-sidebar-polish-and-verification/03-02-SUMMARY.md` contains `No matching automations`.
    - `.planning/phases/03-sidebar-polish-and-verification/03-02-SUMMARY.md` contains `Try a different search.`
    - `.planning/phases/03-sidebar-polish-and-verification/03-02-SUMMARY.md` contains `not selectable`.
    - `.planning/phases/03-sidebar-polish-and-verification/03-02-SUMMARY.md` contains `clearing search restores grouped rows`.
    - `.planning/phases/03-sidebar-polish-and-verification/03-02-SUMMARY.md` contains `Up/Down navigation`.
  </acceptance_criteria>
  <done>Manual UI evidence is recorded in the phase summary without adding new docs or scripts.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `./script/ci.sh` exits 0, or any unrelated/pre-existing failure is isolated with exact evidence and acceptance impact.
- [ ] `03-02-SUMMARY.md` records the CI command and result.
- [ ] `03-02-SUMMARY.md` records source-level checks for row content, empty-state copy, search ownership, navigation source, and no scanner IO.
- [ ] `03-02-SUMMARY.md` records the manual sidebar behavior check under `Manual sidebar behavior check`.
- [ ] No new checklist document, verification script, scheduler mutation, or scanner behavior is added.
</verification>

<success_criteria>
- QUAL-04 is satisfied when `./script/ci.sh` exits 0.
- If CI is blocked by unrelated pre-existing changes, the blocker is isolated and clearly reported.
- ORG-04 remains satisfied after polish because rows retain display name, subtitle, health dot, and selected styling.
- SRCH-03 remains satisfied because search no-results uses a non-selectable inline empty state.
- Clearing search restores grouped keyboard navigation over visible rows.
</success_criteria>

<output>
After completion, create `.planning/phases/03-sidebar-polish-and-verification/03-02-SUMMARY.md`.
</output>
